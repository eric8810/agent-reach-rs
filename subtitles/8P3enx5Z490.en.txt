So, there's this Chrome extension So, there's this Chrome extension So, there's this Chrome extension floating around that basically puts a floating around that basically puts a floating around that basically puts a full AI agent inside your browser, and full AI agent inside your browser, and full AI agent inside your browser, and the entire thing runs locally on your the entire thing runs locally on your the entire thing runs locally on your machine. No API key, no cloud, no machine. No API key, no cloud, no machine. No API key, no cloud, no servers. The model lives on your hard servers. The model lives on your hard servers. The model lives on your hard drive and does inference using your own drive and does inference using your own drive and does inference using your own GPU. It's called the transformers.js GPU. It's called the transformers.js GPU. It's called the transformers.js Gemma 4 browser assistant, and the Gemma 4 browser assistant, and the Gemma 4 browser assistant, and the person behind it is Nico Martin. He's a person behind it is Nico Martin. He's a person behind it is Nico Martin. He's a machine learning engineer at Hugging machine learning engineer at Hugging machine learning engineer at Hugging Face and also a Google Developer Expert Face and also a Google Developer Expert Face and also a Google Developer Expert in AI and web technologies. He literally in AI and web technologies. He literally in AI and web technologies. He literally helps maintain the library that this helps maintain the library that this helps maintain the library that this extension is built on top of. So, this extension is built on top of. So, this extension is built on top of. So, this isn't some random side project from a isn't some random side project from a isn't some random side project from a hobbyist. This is the guy who works on hobbyist. This is the guy who works on hobbyist. This is the guy who works on the underlying tech building a reference the underlying tech building a reference the underlying tech building a reference implementation to show what's possible. implementation to show what's possible. implementation to show what's possible. Also, the official Google Gemma X Also, the official Google Gemma X Also, the official Google Gemma X account also posted about the extension account also posted about the extension account also posted about the extension calling it a completely local agent that calling it a completely local agent that calling it a completely local agent that lives right inside your browser powered lives right inside your browser powered lives right inside your browser powered by Gemma 4 E2B and WebGPU. It uses by Gemma 4 E2B and WebGPU. It uses by Gemma 4 E2B and WebGPU. It uses native tool calling for searching native tool calling for searching native tool calling for searching browser history, reading and summarizing browser history, reading and summarizing browser history, reading and summarizing pages, and managing tabs. pages, and managing tabs. pages, and managing tabs. So, the project has a quiet but So, the project has a quiet but So, the project has a quiet but legitimate stamp of approval from the legitimate stamp of approval from the legitimate stamp of approval from the Gemma team itself. So, the most browser Gemma team itself. So, the most browser Gemma team itself. So, the most browser AI assistants today work the same way. AI assistants today work the same way. AI assistants today work the same way. You install the extension, it sends You install the extension, it sends You install the extension, it sends every page you visit to some server every page you visit to some server every page you visit to some server somewhere. The server runs a big somewhere. The server runs a big somewhere. The server runs a big language model and sends back the language model and sends back the language model and sends back the answer. That's fine for capability, but answer. That's fine for capability, but answer. That's fine for capability, but it's terrible for privacy. Your browsing it's terrible for privacy. Your browsing it's terrible for privacy. Your browsing data, your tabs, your search history, data, your tabs, your search history, data, your tabs, your search history, all of it leaves your machine. It also all of it leaves your machine. It also all of it leaves your machine. It also costs money to run because somebody has costs money to run because somebody has costs money to run because somebody has to pay for those GPU servers, which is to pay for those GPU servers, which is to pay for those GPU servers, which is why most of these tools eventually move why most of these tools eventually move why most of these tools eventually move behind a paywall. And finally, it behind a paywall. And finally, it behind a paywall. And finally, it doesn't work offline. The moment your doesn't work offline. The moment your doesn't work offline. The moment your Wi-Fi dies, your AI assistant dies with Wi-Fi dies, your AI assistant dies with Wi-Fi dies, your AI assistant dies with it. What Nico built does the opposite. it. What Nico built does the opposite. it. What Nico built does the opposite. The model file is downloaded once and The model file is downloaded once and The model file is downloaded once and then sits in your browser's storage. then sits in your browser's storage. then sits in your browser's storage. Every prompt, every tool call, every Every prompt, every tool call, every Every prompt, every tool call, every page summary happens on your own page summary happens on your own page summary happens on your own hardware. The extension never makes a hardware. The extension never makes a hardware. The extension never makes a network request to a model server network request to a model server network request to a model server because there is no model server, just because there is no model server, just because there is no model server, just you and a 3 GB model file. you and a 3 GB model file. you and a 3 GB model file. Okay, so let's talk about what's Okay, so let's talk about what's Okay, so let's talk about what's actually under the hood because the actually under the hood because the actually under the hood because the architecture is pretty clever. The brain architecture is pretty clever. The brain architecture is pretty clever. The brain of the operation is Gemma 4 E2B. The E of the operation is Gemma 4 E2B. The E of the operation is Gemma 4 E2B. The E stands for effective parameters, not the stands for effective parameters, not the stands for effective parameters, not the total model size. The model technically total model size. The model technically total model size. The model technically has more weights than 2 billion, but has more weights than 2 billion, but has more weights than 2 billion, but only about 2 billion are active during only about 2 billion are active during only about 2 billion are active during inference because of something called inference because of something called inference because of something called per layer embeddings. per layer embeddings. per layer embeddings. This trick lets the model stay small at This trick lets the model stay small at This trick lets the model stay small at inference time while still keeping a lot inference time while still keeping a lot inference time while still keeping a lot of knowledge baked in. It supports a of knowledge baked in. It supports a of knowledge baked in. It supports a 128k token context window, native 128k token context window, native 128k token context window, native function calling, and handles 35 plus function calling, and handles 35 plus function calling, and handles 35 plus languages out of the box. It's designed languages out of the box. It's designed languages out of the box. It's designed specifically to run on phones, laptops, specifically to run on phones, laptops, specifically to run on phones, laptops, and yes, browsers. and yes, browsers. and yes, browsers. The extension actually uses two models, The extension actually uses two models, The extension actually uses two models, not one. The first one is the Gemma 4 not one. The first one is the Gemma 4 not one. The first one is the Gemma 4 E2B instruct version. That's the E2B instruct version. That's the E2B instruct version. That's the reasoning brain. It decides which tool reasoning brain. It decides which tool reasoning brain. It decides which tool to call, generates the responses, and to call, generates the responses, and to call, generates the responses, and runs the agent loop. The second model is runs the agent loop. The second model is runs the agent loop. The second model is Mini LM L6 V2, which is a tiny embedding Mini LM L6 V2, which is a tiny embedding Mini LM L6 V2, which is a tiny embedding model. It's only job is to convert text model. It's only job is to convert text model. It's only job is to convert text into vectors so the extension can do into vectors so the extension can do into vectors so the extension can do semantic search across your browsing semantic search across your browsing semantic search across your browsing history and across pages you're reading. history and across pages you're reading. history and across pages you're reading. So, when you ask the agent to find So, when you ask the agent to find So, when you ask the agent to find something on the current page, it's not something on the current page, it's not something on the current page, it's not just doing a keyword match, it's just doing a keyword match, it's just doing a keyword match, it's actually understanding meaning. actually understanding meaning. actually understanding meaning. Both models are quantized down to run on Both models are quantized down to run on Both models are quantized down to run on consumer hardware. Gemma 4 uses Q4F16 consumer hardware. Gemma 4 uses Q4F16 consumer hardware. Gemma 4 uses Q4F16 quantization, which means the weights quantization, which means the weights quantization, which means the weights are compressed to 4 bits with float 16 are compressed to 4 bits with float 16 are compressed to 4 bits with float 16 precision for activations. That's how precision for activations. That's how precision for activations. That's how you get a model that would normally need you get a model that would normally need you get a model that would normally need a workstation GPU to fit comfortably in a workstation GPU to fit comfortably in a workstation GPU to fit comfortably in browser memory. browser memory. browser memory. The whole thing runs through The whole thing runs through The whole thing runs through transformers.js, transformers.js, transformers.js, which is the JavaScript port of Hugging which is the JavaScript port of Hugging which is the JavaScript port of Hugging Face's Python Transformers library. It Face's Python Transformers library. It Face's Python Transformers library. It uses WebGPU for hardware acceleration, uses WebGPU for hardware acceleration, uses WebGPU for hardware acceleration, so as long as your browser supports it, so as long as your browser supports it, so as long as your browser supports it, you get GPU inference for free. The you get GPU inference for free. The you get GPU inference for free. The architecture inside the extension is architecture inside the extension is architecture inside the extension is split into three layers. There's a split into three layers. There's a split into three layers. There's a background service worker that hosts the background service worker that hosts the background service worker that hosts the models and runs the agent loop. There's models and runs the agent loop. There's models and runs the agent loop. There's a side panel, which is the chat UI you a side panel, which is the chat UI you a side panel, which is the chat UI you actually talk to. And there's a content actually talk to. And there's a content actually talk to. And there's a content script that lives inside whatever script that lives inside whatever script that lives inside whatever webpage you have open handling DOM webpage you have open handling DOM webpage you have open handling DOM extraction and highlighting elements. extraction and highlighting elements. extraction and highlighting elements. The reason for splitting things this way The reason for splitting things this way The reason for splitting things this way is that Chrome's Manifest V3 has strict is that Chrome's Manifest V3 has strict is that Chrome's Manifest V3 has strict rules about where heavy work can happen. rules about where heavy work can happen. rules about where heavy work can happen. By keeping the model in the background By keeping the model in the background By keeping the model in the background worker, the model only loads once no worker, the model only loads once no worker, the model only loads once no matter how many tabs you have open. The matter how many tabs you have open. The matter how many tabs you have open. The side panel and content scripts are kept side panel and content scripts are kept side panel and content scripts are kept thin just sending messages back and thin just sending messages back and thin just sending messages back and forth. forth. forth. For the agent loop itself, Nico built a For the agent loop itself, Nico built a For the agent loop itself, Nico built a tool calling layer he calls WebMCP, tool calling layer he calls WebMCP, tool calling layer he calls WebMCP, which normalizes browser tools into a which normalizes browser tools into a which normalizes browser tools into a shape the model can understand. Each shape the model can understand. Each shape the model can understand. Each tool has a name, description, input tool has a name, description, input tool has a name, description, input schema, and an execute function. When schema, and an execute function. When schema, and an execute function. When you type a prompt, the model decides if you type a prompt, the model decides if you type a prompt, the model decides if it needs a tool, emits a special tool it needs a tool, emits a special tool it needs a tool, emits a special tool called token block, the parser pulls called token block, the parser pulls called token block, the parser pulls that out, runs the actual function, then that out, runs the actual function, then that out, runs the actual function, then feeds the result back to the model for feeds the result back to the model for feeds the result back to the model for another turn. This loops until the model another turn. This loops until the model another turn. This loops until the model has nothing left to call. The tools has nothing left to call. The tools has nothing left to call. The tools available in the extension include get available in the extension include get available in the extension include get open tabs, go to tab, open URL, close open tabs, go to tab, open URL, close open tabs, go to tab, open URL, close tab, find history, ask website, and tab, find history, ask website, and tab, find history, ask website, and highlight website element. Pretty highlight website element. Pretty highlight website element. Pretty practical stuff for navigating the web. practical stuff for navigating the web. practical stuff for navigating the web. All right, let me actually install this All right, let me actually install this All right, let me actually install this thing and try it out. I'll go to the thing and try it out. I'll go to the thing and try it out. I'll go to the Chrome Web Store, search for Chrome Web Store, search for Chrome Web Store, search for transformers.js transformers.js transformers.js Gemma 4 browser assistant, and hit Gemma 4 browser assistant, and hit Gemma 4 browser assistant, and hit install. The extension installs in 2 install. The extension installs in 2 install. The extension installs in 2 seconds because it's tiny. The actual seconds because it's tiny. The actual seconds because it's tiny. The actual model isn't bundled with the extension, model isn't bundled with the extension, model isn't bundled with the extension, it gets downloaded the first time you it gets downloaded the first time you it gets downloaded the first time you open the side panel. So, I click the open the side panel. So, I click the open the side panel. So, I click the extension icon, the side panel pops out extension icon, the side panel pops out extension icon, the side panel pops out on the right, and right away it's asking on the right, and right away it's asking on the right, and right away it's asking me to download the model. I hit download me to download the model. I hit download me to download the model. I hit download and the progress bar starts moving. and the progress bar starts moving. and the progress bar starts moving. Total download size is about 3 GB Total download size is about 3 GB Total download size is about 3 GB combined. Like I said, it's pulling the combined. Like I said, it's pulling the combined. Like I said, it's pulling the Gemma 4 E2B and the Mini LM embedding Gemma 4 E2B and the Mini LM embedding Gemma 4 E2B and the Mini LM embedding model. This part takes a few minutes model. This part takes a few minutes model. This part takes a few minutes depending on your connection. Once it's depending on your connection. Once it's depending on your connection. Once it's done, the model is cached in your done, the model is cached in your done, the model is cached in your browser's IndexedDB storage under the browser's IndexedDB storage under the browser's IndexedDB storage under the extension origin, so you only download extension origin, so you only download extension origin, so you only download it once. After this, even if you go it once. After this, even if you go it once. After this, even if you go offline, the agent still works. offline, the agent still works. offline, the agent still works. For anyone wondering, you don't have to For anyone wondering, you don't have to For anyone wondering, you don't have to use the Chrome Web Store version. The use the Chrome Web Store version. The use the Chrome Web Store version. The full source code is on GitHub as well. full source code is on GitHub as well. full source code is on GitHub as well. You can clone the repo, run pnpm You can clone the repo, run pnpm You can clone the repo, run pnpm install, and pnpm run dev, and load it install, and pnpm run dev, and load it install, and pnpm run dev, and load it as an unpacked extension. That's the as an unpacked extension. That's the as an unpacked extension. That's the path if you want to modify the agent, path if you want to modify the agent, path if you want to modify the agent, add your own tools, or just see how the add your own tools, or just see how the add your own tools, or just see how the whole thing is wired up. Okay, model is whole thing is wired up. Okay, model is whole thing is wired up. Okay, model is loaded. Time to actually test this loaded. Time to actually test this loaded. Time to actually test this thing. First test, I'm going to ask it thing. First test, I'm going to ask it thing. First test, I'm going to ask it to summarize the current webpage. I have to summarize the current webpage. I have to summarize the current webpage. I have a long blog post open about web a long blog post open about web a long blog post open about web development. I type, "Summarize this development. I type, "Summarize this development. I type, "Summarize this page in three sentences." The agent page in three sentences." The agent page in three sentences." The agent fires off the ask website tool. Behind fires off the ask website tool. Behind fires off the ask website tool. Behind the scenes, the content script grabs the the scenes, the content script grabs the the scenes, the content script grabs the headings, paragraphs, and lists from the headings, paragraphs, and lists from the headings, paragraphs, and lists from the DOM, runs them through the embedding DOM, runs them through the embedding DOM, runs them through the embedding model, and pulls the most relevant model, and pulls the most relevant model, and pulls the most relevant chunks. chunks. chunks. Then Gemma 4 reads those chunks and Then Gemma 4 reads those chunks and Then Gemma 4 reads those chunks and writes a clean three-sentence summary. writes a clean three-sentence summary. writes a clean three-sentence summary. Took about 20 seconds on my machine, Took about 20 seconds on my machine, Took about 20 seconds on my machine, which has a decent GPU. which has a decent GPU. which has a decent GPU. Second, I'm going to ask it about my Second, I'm going to ask it about my Second, I'm going to ask it about my browsing history. I type, "Find that browsing history. I type, "Find that browsing history. I type, "Find that article I read last week about building article I read last week about building article I read last week about building effective AI agents." The agent calls effective AI agents." The agent calls effective AI agents." The agent calls the find history tool. It searches the find history tool. It searches the find history tool. It searches against the semantic vector database of against the semantic vector database of against the semantic vector database of pages you've visited, not just the URLs pages you've visited, not just the URLs pages you've visited, not just the URLs and titles, but the actual content. It and titles, but the actual content. It and titles, but the actual content. It shows me the titles and descriptions and shows me the titles and descriptions and shows me the titles and descriptions and asks if I want to open it. That's asks if I want to open it. That's asks if I want to open it. That's actually really useful because most actually really useful because most actually really useful because most browser history search is just keyword browser history search is just keyword browser history search is just keyword matching against page titles, which matching against page titles, which matching against page titles, which fails the moment you can't remember the fails the moment you can't remember the fails the moment you can't remember the exact title. The agent has three main exact title. The agent has three main exact title. The agent has three main groups of capabilities, and they all groups of capabilities, and they all groups of capabilities, and they all work in combination. The first group is work in combination. The first group is work in combination. The first group is tab management. The agent can list every tab management. The agent can list every tab management. The agent can list every open tab with titles and URLs, switch open tab with titles and URLs, switch open tab with titles and URLs, switch you to a specific tab, open new URLs in you to a specific tab, open new URLs in you to a specific tab, open new URLs in either the foreground or background, and either the foreground or background, and either the foreground or background, and close tabs you don't want anymore. close tabs you don't want anymore. close tabs you don't want anymore. So, instead of manually digging through So, instead of manually digging through So, instead of manually digging through 20 open tabs to find the one with your 20 open tabs to find the one with your 20 open tabs to find the one with your bank login, you can just say, "Switch to bank login, you can just say, "Switch to bank login, you can just say, "Switch to my banking tab," and it'll find it. The my banking tab," and it'll find it. The my banking tab," and it'll find it. The second group is website interaction, second group is website interaction, second group is website interaction, which is where the retrieval augmented which is where the retrieval augmented which is where the retrieval augmented generation comes in. generation comes in. generation comes in. The ask website tool I showed earlier is The ask website tool I showed earlier is The ask website tool I showed earlier is part of this, but there's also a part of this, but there's also a part of this, but there's also a highlight website element tool. So, you highlight website element tool. So, you highlight website element tool. So, you can ask the agent to find the section can ask the agent to find the section can ask the agent to find the section about pricing on a long product page, about pricing on a long product page, about pricing on a long product page, and it'll actually scroll the page to and it'll actually scroll the page to and it'll actually scroll the page to that section and highlight the relevant that section and highlight the relevant that section and highlight the relevant paragraph for you. The third group is paragraph for you. The third group is paragraph for you. The third group is the history vector database, which is the history vector database, which is the history vector database, which is honestly the feature I find the most honestly the feature I find the most honestly the feature I find the most interesting. Every page you visit gets interesting. Every page you visit gets interesting. Every page you visit gets stored as a vector embedding in a local stored as a vector embedding in a local stored as a vector embedding in a local IndexedDB database, including the title, IndexedDB database, including the title, IndexedDB database, including the title, description, and URL. Then when you description, and URL. Then when you description, and URL. Then when you search, you're searching by meaning, not search, you're searching by meaning, not search, you're searching by meaning, not by exact text match. So, if you read an by exact text match. So, if you read an by exact text match. So, if you read an article 2 weeks ago about reducing API article 2 weeks ago about reducing API article 2 weeks ago about reducing API costs, but you can't remember the title costs, but you can't remember the title costs, but you can't remember the title or the website, you can just ask the or the website, you can just ask the or the website, you can just ask the agent to find that thing about saving agent to find that thing about saving agent to find that thing about saving money on cloud infrastructure, and it'll money on cloud infrastructure, and it'll money on cloud infrastructure, and it'll surface it. The performance honestly surface it. The performance honestly surface it. The performance honestly depends on your hardware. On a recent depends on your hardware. On a recent depends on your hardware. On a recent MacBook with an M3 or M4 chip, you'll MacBook with an M3 or M4 chip, you'll MacBook with an M3 or M4 chip, you'll get something like 15 to 25 tokens per get something like 15 to 25 tokens per get something like 15 to 25 tokens per second. On a Windows machine with a second. On a Windows machine with a second. On a Windows machine with a dedicated GPU, similar range. On an dedicated GPU, similar range. On an dedicated GPU, similar range. On an older laptop without WebGPU support or older laptop without WebGPU support or older laptop without WebGPU support or with integrated graphics, it'll be with integrated graphics, it'll be with integrated graphics, it'll be slower, maybe five tokens per second. It slower, maybe five tokens per second. It slower, maybe five tokens per second. It still works, just less snappy. The still works, just less snappy. The still works, just less snappy. The bigger picture here is that this whole bigger picture here is that this whole bigger picture here is that this whole category of tools is about to explode. category of tools is about to explode. category of tools is about to explode. Models small enough to run in a browser Models small enough to run in a browser Models small enough to run in a browser used to be useless. Now you've got a 2B used to be useless. Now you've got a 2B used to be useless. Now you've got a 2B effective parameter model that handles effective parameter model that handles effective parameter model that handles tool calling, multi-step reasoning, and tool calling, multi-step reasoning, and tool calling, multi-step reasoning, and semantic search well enough to be semantic search well enough to be semantic search well enough to be useful. The same architecture Nico built useful. The same architecture Nico built useful. The same architecture Nico built can be extended into per tab agents, can be extended into per tab agents, can be extended into per tab agents, options pages, or pop-up based options pages, or pop-up based options pages, or pop-up based assistants. And because everything is assistants. And because everything is assistants. And because everything is local, you can use it on internal local, you can use it on internal local, you can use it on internal company pages, banking sites, or company pages, banking sites, or company pages, banking sites, or anywhere you'd never paste your data anywhere you'd never paste your data anywhere you'd never paste your data into ChatGPT. All right, so that's it into ChatGPT. All right, so that's it into ChatGPT. All right, so that's it from the video and I hope you enjoyed from the video and I hope you enjoyed from the video and I hope you enjoyed it. If you did, please like this video it. If you did, please like this video it. If you did, please like this video and subscribe to the channel and I'll and subscribe to the channel and I'll and subscribe to the channel and I'll see you in the next video.
