AI is overwhelming and this course is AI is overwhelming and this course is AI is overwhelming and this course is our humble effort to make it a little our humble effort to make it a little our humble effort to make it a little less intimidating and maybe have a less intimidating and maybe have a less intimidating and maybe have a little fun while we're at it. My name is little fun while we're at it. My name is little fun while we're at it. My name is Momshad Manambat and I'll be your Momshad Manambat and I'll be your Momshad Manambat and I'll be your instructor for this course. instructor for this course. instructor for this course. &gt;&gt; [snorts] &gt;&gt; [snorts] &gt;&gt; [snorts] &gt;&gt; We start from the absolute basics of AI &gt;&gt; We start from the absolute basics of AI &gt;&gt; We start from the absolute basics of AI and work our way up to understanding and work our way up to understanding and work our way up to understanding what AI agents are. Along the way, we'll what AI agents are. Along the way, we'll what AI agents are. Along the way, we'll dissect Open Claw, a popular open-source dissect Open Claw, a popular open-source dissect Open Claw, a popular open-source AI agent that has gained a lot of AI agent that has gained a lot of AI agent that has gained a lot of interest recently to see exactly what interest recently to see exactly what interest recently to see exactly what made it tick. But this isn't just made it tick. But this isn't just made it tick. But this isn't just another theory-only course. You'll get another theory-only course. You'll get another theory-only course. You'll get hands-on access to our lab environments hands-on access to our lab environments hands-on access to our lab environments where you practice building agents where you practice building agents where you practice building agents yourself. Stress-free without worrying yourself. Stress-free without worrying yourself. Stress-free without worrying about unexpected charges. We provide about unexpected charges. We provide about unexpected charges. We provide everything you need from API keys to everything you need from API keys to everything you need from API keys to sandbox cloud environments so you can sandbox cloud environments so you can sandbox cloud environments so you can focus purely on learning. focus purely on learning. focus purely on learning. Throughout this course, you will build Throughout this course, you will build Throughout this course, you will build four AI agents. I call them Zippy, four AI agents. I call them Zippy, four AI agents. I call them Zippy, Savvy, Meshy, and Cody. Pardon the Savvy, Meshy, and Cody. Pardon the Savvy, Meshy, and Cody. Pardon the choice of names. I thought they'd go choice of names. I thought they'd go choice of names. I thought they'd go well with the with the vibe. well with the with the vibe. well with the with the vibe. You'll see Zippy's journey from being a You'll see Zippy's journey from being a You'll see Zippy's journey from being a solo generic agent to becoming an solo generic agent to becoming an solo generic agent to becoming an orchestrator that manages a multi-agent orchestrator that manages a multi-agent orchestrator that manages a multi-agent setup of Savvy, a research agent, Meshy, setup of Savvy, a research agent, Meshy, setup of Savvy, a research agent, Meshy, a memory agent, and Cody, a coding a memory agent, and Cody, a coding a memory agent, and Cody, a coding agent. agent. agent. Here's how the course is structured. We Here's how the course is structured. We Here's how the course is structured. We start with fundamentals, the building start with fundamentals, the building start with fundamentals, the building blocks of AI. blocks of AI. blocks of AI. You'll learn what ChatGPT actually is, You'll learn what ChatGPT actually is, You'll learn what ChatGPT actually is, how large language models work, what how large language models work, what how large language models work, what tokens and tokenization mean, how tokens and tokenization mean, how tokens and tokenization mean, how temperature and sampling shape temperature and sampling shape temperature and sampling shape responses, and how to write effective responses, and how to write effective responses, and how to write effective prompts. From there, we move into prompts. From there, we move into prompts. From there, we move into architecture, how AI systems are architecture, how AI systems are architecture, how AI systems are assembled from parts. You'll learn what assembled from parts. You'll learn what assembled from parts. You'll learn what tools are and how to integrate them, tools are and how to integrate them, tools are and how to integrate them, building AI workflows, and understand building AI workflows, and understand building AI workflows, and understand the key distinction that most people the key distinction that most people the key distinction that most people miss. miss. miss. What separates a workflow from an agent? What separates a workflow from an agent? What separates a workflow from an agent? Then comes implementation where you Then comes implementation where you Then comes implementation where you build real agents. You'll learn what an build real agents. You'll learn what an build real agents. You'll learn what an agent actually is, how agent memory agent actually is, how agent memory agent actually is, how agent memory works, planning and reasoning works, planning and reasoning works, planning and reasoning strategies, multi-agent systems, and strategies, multi-agent systems, and strategies, multi-agent systems, and error handling patterns. You'll also error handling patterns. You'll also error handling patterns. You'll also explore coding agents and the design explore coding agents and the design explore coding agents and the design patterns that Anthropic and Google use patterns that Anthropic and Google use patterns that Anthropic and Google use in production. We close with the Open in production. We close with the Open in production. We close with the Open Claw case study where we dissect a real Claw case study where we dissect a real Claw case study where we dissect a real open-source AI agent. You'll walk open-source AI agent. You'll walk open-source AI agent. You'll walk through Open Claw's architecture and through Open Claw's architecture and through Open Claw's architecture and tools, its agent loop, memory system, tools, its agent loop, memory system, tools, its agent loop, memory system, testing strategies, monitoring and testing strategies, monitoring and testing strategies, monitoring and security. What's everyone's concern security. What's everyone's concern security. What's everyone's concern regarding Open Claw's security regarding Open Claw's security regarding Open Claw's security challenges? Is it real? What caused it? challenges? Is it real? What caused it? challenges? Is it real? What caused it? Etc. Etc. Etc. By the end, you'll know how to build, By the end, you'll know how to build, By the end, you'll know how to build, test, and deploy effective AI agents test, and deploy effective AI agents test, and deploy effective AI agents from scratch. from scratch. from scratch. And we're excited to release the first And we're excited to release the first And we're excited to release the first half of this course here on YouTube. So, half of this course here on YouTube. So, half of this course here on YouTube. So, let us begin. Let's start with something you already Let's start with something you already Let's start with something you already know. ChatGPT. know. ChatGPT. know. ChatGPT. The name ChatGPT is actually two parts. The name ChatGPT is actually two parts. The name ChatGPT is actually two parts. Chat stands for the chat application and Chat stands for the chat application and Chat stands for the chat application and GPT is the AI technology that powers it. GPT is the AI technology that powers it. GPT is the AI technology that powers it. GPT is the brain and chat is the GPT is the brain and chat is the GPT is the brain and chat is the interface, the way you interact with the interface, the way you interact with the interface, the way you interact with the brain. brain. brain. GPT stands for generative pre-trained GPT stands for generative pre-trained GPT stands for generative pre-trained transformer. Let's take it apart one transformer. Let's take it apart one transformer. Let's take it apart one word at a time and reverse order because word at a time and reverse order because word at a time and reverse order because it's easier to understand that way. it's easier to understand that way. it's easier to understand that way. A transformer is a specific design for A transformer is a specific design for A transformer is a specific design for how an AI system processes language. It how an AI system processes language. It how an AI system processes language. It was invented in 2017 by researchers at was invented in 2017 by researchers at was invented in 2017 by researchers at Google. Google. Google. AI existed long before 2017. Many of us AI existed long before 2017. Many of us AI existed long before 2017. Many of us had already used some form of it. had already used some form of it. had already used some form of it. Chatbots, spam filters, auto-correct, Chatbots, spam filters, auto-correct, Chatbots, spam filters, auto-correct, but it never felt truly intelligent. So, but it never felt truly intelligent. So, but it never felt truly intelligent. So, what changed? At its core, AI works by what changed? At its core, AI works by what changed? At its core, AI works by predicting what comes next. Given some predicting what comes next. Given some predicting what comes next. Given some input, it makes its best guess about input, it makes its best guess about input, it makes its best guess about what follows. That basic idea hadn't what follows. That basic idea hadn't what follows. That basic idea hadn't changed. What changed was how well AI changed. What changed was how well AI changed. What changed was how well AI could do it, especially with language. could do it, especially with language. could do it, especially with language. In 2017, a team of researchers at Google In 2017, a team of researchers at Google In 2017, a team of researchers at Google published a paper called Attention Is published a paper called Attention Is published a paper called Attention Is All You Need. It introduced a new All You Need. It introduced a new All You Need. It introduced a new architecture called the transformer and architecture called the transformer and architecture called the transformer and it quietly changed everything. You don't it quietly changed everything. You don't it quietly changed everything. You don't need to understand the technical details need to understand the technical details need to understand the technical details right now. What matters is this. Before right now. What matters is this. Before right now. What matters is this. Before that, computers were bad at that, computers were bad at that, computers were bad at understanding language. understanding language. understanding language. After that, they got dramatically After that, they got dramatically After that, they got dramatically better. better. better. The key superpower of transformers is The key superpower of transformers is The key superpower of transformers is attention, the ability to look at is attention, the ability to look at is attention, the ability to look at is attention, the ability to look at all attention, the ability to look at all attention, the ability to look at all the words in a sentence at once and the words in a sentence at once and the words in a sentence at once and understand how they relate to each understand how they relate to each understand how they relate to each other. For example, consider this other. For example, consider this other. For example, consider this sentence. The dog chased its tail sentence. The dog chased its tail sentence. The dog chased its tail because it was bored. What does it refer because it was bored. What does it refer because it was bored. What does it refer to here? to here? to here? The dog or the tail? You instantly know The dog or the tail? You instantly know The dog or the tail? You instantly know it's the dog. Earlier AI systems it's the dog. Earlier AI systems it's the dog. Earlier AI systems struggled with this. Transformers handle struggled with this. Transformers handle struggled with this. Transformers handle it easily because they can pay attention it easily because they can pay attention it easily because they can pay attention to the relationships between all the to the relationships between all the to the relationships between all the words simultaneously. words simultaneously. words simultaneously. So, that's what transformers are. So, that's what transformers are. So, that's what transformers are. Now, let's look at the second word. The Now, let's look at the second word. The Now, let's look at the second word. The pre-trained part means GPT learned an pre-trained part means GPT learned an pre-trained part means GPT learned an enormous amount before it ever talked to enormous amount before it ever talked to enormous amount before it ever talked to a single user. a single user. a single user. Here's how that works. First, OpenAI or Here's how that works. First, OpenAI or Here's how that works. First, OpenAI or the company training the model collects the company training the model collects the company training the model collects billions of pages of text from books, billions of pages of text from books, billions of pages of text from books, websites, articles, code, and more. websites, articles, code, and more. websites, articles, code, and more. Imagine a library bigger than any Imagine a library bigger than any Imagine a library bigger than any library that has ever existed. That's library that has ever existed. That's library that has ever existed. That's roughly the scale of GPT's training roughly the scale of GPT's training roughly the scale of GPT's training data. data. data. GPT then read all of this text and GPT then read all of this text and GPT then read all of this text and learned patterns. Not facts, not learned patterns. Not facts, not learned patterns. Not facts, not opinions, patterns. For example, after opinions, patterns. For example, after opinions, patterns. For example, after "The capital of France is", the word "The capital of France is", the word "The capital of France is", the word "Paris" usually comes next. After "Once "Paris" usually comes next. After "Once "Paris" usually comes next. After "Once upon a time", a story usually follows. upon a time", a story usually follows. upon a time", a story usually follows. Technical questions tend to get Technical questions tend to get Technical questions tend to get structured answers and casual greetings structured answers and casual greetings structured answers and casual greetings get casual responses. get casual responses. get casual responses. And the core of how GPT actually learns And the core of how GPT actually learns And the core of how GPT actually learns is by predicting the next word. During is by predicting the next word. During is by predicting the next word. During training, GPT was given billions of training, GPT was given billions of training, GPT was given billions of sentences with the last word hidden and sentences with the last word hidden and sentences with the last word hidden and it had to guess what came next. Given it had to guess what came next. Given it had to guess what came next. Given "The cat sat on the", "The cat sat on the", "The cat sat on the", the correct answer is mat. Given "To be the correct answer is mat. Given "To be the correct answer is mat. Given "To be or not to", it's be. Given "Water boils or not to", it's be. Given "Water boils or not to", it's be. Given "Water boils at 100掳", it's Celsius. at 100掳", it's Celsius. at 100掳", it's Celsius. It got it wrong a lot at first, but It got it wrong a lot at first, but It got it wrong a lot at first, but after seeing billions of examples, it after seeing billions of examples, it after seeing billions of examples, it got incredibly good at predicting what got incredibly good at predicting what got incredibly good at predicting what comes next. Not just simple sentences, comes next. Not just simple sentences, comes next. Not just simple sentences, but entire paragraphs across every topic but entire paragraphs across every topic but entire paragraphs across every topic imaginable. imaginable. imaginable. This is also why hallucinations happen. This is also why hallucinations happen. This is also why hallucinations happen. GPT isn't looking up facts, it's GPT isn't looking up facts, it's GPT isn't looking up facts, it's generating text that sounds like it generating text that sounds like it generating text that sounds like it should be correct based on patterns. And should be correct based on patterns. And should be correct based on patterns. And sometimes those patterns lead to astray. sometimes those patterns lead to astray. sometimes those patterns lead to astray. And finally, the generative part is the And finally, the generative part is the And finally, the generative part is the most straightforward. It means GPT most straightforward. It means GPT most straightforward. It means GPT creates new text rather than copying creates new text rather than copying creates new text rather than copying text from somewhere else. text from somewhere else. text from somewhere else. When you ask GPT a question, it doesn't When you ask GPT a question, it doesn't When you ask GPT a question, it doesn't search a database for a pre-written search a database for a pre-written search a database for a pre-written answer. It generates a brand new answer. It generates a brand new answer. It generates a brand new response word by word based on patterns response word by word based on patterns response word by word based on patterns it learned during pre-training. it learned during pre-training. it learned during pre-training. GPT is a family of models that have GPT is a family of models that have GPT is a family of models that have gotten more capable over time. GPT-1 gotten more capable over time. GPT-1 gotten more capable over time. GPT-1 came out in 2018 and proved that the came out in 2018 and proved that the came out in 2018 and proved that the concept works. GPT-2 followed in 2019 concept works. GPT-2 followed in 2019 concept works. GPT-2 followed in 2019 and was good enough that OpenAI and was good enough that OpenAI and was good enough that OpenAI initially worried about misuse and GPT-3 initially worried about misuse and GPT-3 initially worried about misuse and GPT-3 launched in 2020 and was a real launched in 2020 and was a real launched in 2020 and was a real breakthrough. breakthrough. breakthrough. It could write essays, code, and more. It could write essays, code, and more. It could write essays, code, and more. GPT-3.5 arrived in 2022 and powered the GPT-3.5 arrived in 2022 and powered the GPT-3.5 arrived in 2022 and powered the original ChatGPT launch. GPT-4 came in original ChatGPT launch. GPT-4 came in original ChatGPT launch. GPT-4 came in 2023 and was much more capable with the 2023 and was much more capable with the 2023 and was much more capable with the ability to understand images, too. And ability to understand images, too. And ability to understand images, too. And GPT-4o launched in 2024, faster and with GPT-4o launched in 2024, faster and with GPT-4o launched in 2024, faster and with better voice and vision capabilities. better voice and vision capabilities. better voice and vision capabilities. Each [snorts] version was bigger, more Each [snorts] version was bigger, more Each [snorts] version was bigger, more training data, more computing power, and training data, more computing power, and training data, more computing power, and better at predicting the next word. And better at predicting the next word. And better at predicting the next word. And GPT is just one family of LLMs. Other GPT is just one family of LLMs. Other GPT is just one family of LLMs. Other companies have built their own. Claude companies have built their own. Claude companies have built their own. Claude from Anthropic, Gemini from Google, and from Anthropic, Gemini from Google, and from Anthropic, Gemini from Google, and Llama from Meta, which is open-source, Llama from Meta, which is open-source, Llama from Meta, which is open-source, meaning anyone can use it. They all meaning anyone can use it. They all meaning anyone can use it. They all share the same basic idea, transformer share the same basic idea, transformer share the same basic idea, transformer architecture pre-trained on text architecture pre-trained on text architecture pre-trained on text generating responses word by word. generating responses word by word. generating responses word by word. Now, it's important to note that the Now, it's important to note that the Now, it's important to note that the transformer architecture is used beyond transformer architecture is used beyond transformer architecture is used beyond language. Sora, which generates video, language. Sora, which generates video, language. Sora, which generates video, and various audio models also use and various audio models also use and various audio models also use transformers, but they're not LLMs transformers, but they're not LLMs transformers, but they're not LLMs because they don't work with language. because they don't work with language. because they don't work with language. The L in LLMs specifically means The L in LLMs specifically means The L in LLMs specifically means language. Now, models like GPT-4o and language. Now, models like GPT-4o and language. Now, models like GPT-4o and Gemini blur this line. They handle texts Gemini blur this line. They handle texts Gemini blur this line. They handle texts and images and audio. So, their core is and images and audio. So, their core is and images and audio. So, their core is still language model, but they've been still language model, but they've been still language model, but they've been extended to understand other types of extended to understand other types of extended to understand other types of input. People still call them LLMs by input. People still call them LLMs by input. People still call them LLMs by convention. Now, let's dig into what makes LLMs Now, let's dig into what makes LLMs Now, let's dig into what makes LLMs large and why that matters. large and why that matters. large and why that matters. An LLM is what you get when you take the An LLM is what you get when you take the An LLM is what you get when you take the transformer architecture, scale it up transformer architecture, scale it up transformer architecture, scale it up with billions of parameters, and train with billions of parameters, and train with billions of parameters, and train it on massive text. Large refers to two it on massive text. Large refers to two it on massive text. Large refers to two things. things. things. Large training data and a large number Large training data and a large number Large training data and a large number of parameters. of parameters. of parameters. To [snorts] put this in perspective, all To [snorts] put this in perspective, all To [snorts] put this in perspective, all of English Wikipedia, every article ever of English Wikipedia, every article ever of English Wikipedia, every article ever written, is about 4 billion words. GPT-3 written, is about 4 billion words. GPT-3 written, is about 4 billion words. GPT-3 was trained on roughly 125 times that was trained on roughly 125 times that was trained on roughly 125 times that amount. Modern LLMs have gone further amount. Modern LLMs have gone further amount. Modern LLMs have gone further still, consuming text from books, still, consuming text from books, still, consuming text from books, websites, code repositories, scientific websites, code repositories, scientific websites, code repositories, scientific papers, and more. papers, and more. papers, and more. Now, if you read one book per week, it Now, if you read one book per week, it Now, if you read one book per week, it would take you 180,000 years to read would take you 180,000 years to read would take you 180,000 years to read what GPT-3 was trained on. what GPT-3 was trained on. what GPT-3 was trained on. Parameters are internal settings the Parameters are internal settings the Parameters are internal settings the model tunes during training to get model tunes during training to get model tunes during training to get better at predicting the next word. More better at predicting the next word. More better at predicting the next word. More parameters means more subtle control and parameters means more subtle control and parameters means more subtle control and better performance. better performance. better performance. But what does that actually mean? Think But what does that actually mean? Think But what does that actually mean? Think of it like learning to throw a of it like learning to throw a of it like learning to throw a basketball. Nobody consciously basketball. Nobody consciously basketball. Nobody consciously calculates the exact angle and force calculates the exact angle and force calculates the exact angle and force for every muscle in their arm. for every muscle in their arm. for every muscle in their arm. You throw, you miss, you adjust, you You throw, you miss, you adjust, you You throw, you miss, you adjust, you throw again. Eventually, your body just throw again. Eventually, your body just throw again. Eventually, your body just knows the right settings. Parameters knows the right settings. Parameters knows the right settings. Parameters work the same way, except with numbers work the same way, except with numbers work the same way, except with numbers instead of muscle memory. instead of muscle memory. instead of muscle memory. Each parameter is just a number, 0.87 or Each parameter is just a number, 0.87 or Each parameter is just a number, 0.87 or -0.23. -0.23. -0.23. These numbers sit at the connections These numbers sit at the connections These numbers sit at the connections between neurons in the network and between neurons in the network and between neurons in the network and control how information flows through control how information flows through control how information flows through it. it. it. When the model is first created, all of When the model is first created, all of When the model is first created, all of these numbers are random. The model is these numbers are random. The model is these numbers are random. The model is essentially useless. It might predict essentially useless. It might predict essentially useless. It might predict that the cat sat on the ends with that the cat sat on the ends with that the cat sat on the ends with refrigerator. refrigerator. refrigerator. So, how does it learn? The model is So, how does it learn? The model is So, how does it learn? The model is shown real text from books, websites, shown real text from books, websites, shown real text from books, websites, and articles and asked to predict the and articles and asked to predict the and articles and asked to predict the next word. It guesses, then it compares next word. It guesses, then it compares next word. It guesses, then it compares its guess to what actually came next in its guess to what actually came next in its guess to what actually came next in the text. the text. the text. If it was wrong, an algorithm nudges If it was wrong, an algorithm nudges If it was wrong, an algorithm nudges every parameter slightly in the every parameter slightly in the every parameter slightly in the direction that would have made the direction that would have made the direction that would have made the answer more correct. Then, it does this answer more correct. Then, it does this answer more correct. Then, it does this again and again and again billions of again and again and again billions of again and again and again billions of times. times. times. No human is writing these numbers or No human is writing these numbers or No human is writing these numbers or grading the answers. The training data grading the answers. The training data grading the answers. The training data itself is the teacher. It already itself is the teacher. It already itself is the teacher. It already contains correct natural language. The contains correct natural language. The contains correct natural language. The model just learns to mimic those model just learns to mimic those model just learns to mimic those patterns through repetition. And after patterns through repetition. And after patterns through repetition. And after billions of rounds, those random numbers billions of rounds, those random numbers billions of rounds, those random numbers have been shaped into an incredibly have been shaped into an incredibly have been shaped into an incredibly precise configuration that understands precise configuration that understands precise configuration that understands language. Again, you don't need to language. Again, you don't need to language. Again, you don't need to understand training in detail to build understand training in detail to build understand training in detail to build AI agents. That's the job of machine AI agents. That's the job of machine AI agents. That's the job of machine learning engineers who create these learning engineers who create these learning engineers who create these models. But knowing how it works helps models. But knowing how it works helps models. But knowing how it works helps you understand why LLMs behave the way you understand why LLMs behave the way you understand why LLMs behave the way they do and why they sometimes get they do and why they sometimes get they do and why they sometimes get things wrong. things wrong. things wrong. Now, to give you a sense of scale, look Now, to give you a sense of scale, look Now, to give you a sense of scale, look at how quickly these models grew. So, at how quickly these models grew. So, at how quickly these models grew. So, GPT-2 had 1.5 billion parameters. GPT-3 GPT-2 had 1.5 billion parameters. GPT-3 GPT-2 had 1.5 billion parameters. GPT-3 jumped to 175 billion parameters. jumped to 175 billion parameters. jumped to 175 billion parameters. The jump from GPT-2 to GPT-3 was roughly The jump from GPT-2 to GPT-3 was roughly The jump from GPT-2 to GPT-3 was roughly 100 times more parameters. That's the 100 times more parameters. That's the 100 times more parameters. That's the difference between a model that could difference between a model that could difference between a model that could write passable text and one that could write passable text and one that could write passable text and one that could write convincing essays, translate write convincing essays, translate write convincing essays, translate languages, and write code. languages, and write code. languages, and write code. Now, bigger isn't always better though. Now, bigger isn't always better though. Now, bigger isn't always better though. More parameters means more computing More parameters means more computing More parameters means more computing power, more cost, and slower responses. power, more cost, and slower responses. power, more cost, and slower responses. And sometimes a smaller, well-trained And sometimes a smaller, well-trained And sometimes a smaller, well-trained model is the better choice for a model is the better choice for a model is the better choice for a specific task. specific task. specific task. Now, LLMs are remarkably good at working Now, LLMs are remarkably good at working Now, LLMs are remarkably good at working with language. They can generate fluent, with language. They can generate fluent, with language. They can generate fluent, human-sounding text, summarize long human-sounding text, summarize long human-sounding text, summarize long documents, translate between languages, documents, translate between languages, documents, translate between languages, write code, follow instructions given in write code, follow instructions given in write code, follow instructions given in plain English, and and shift their tone plain English, and and shift their tone plain English, and and shift their tone to match whatever you need, formal, to match whatever you need, formal, to match whatever you need, formal, casual, technical, playful. casual, technical, playful. casual, technical, playful. But they have real blind spots. So, you But they have real blind spots. So, you But they have real blind spots. So, you ask an LLM to do math and it might get ask an LLM to do math and it might get ask an LLM to do math and it might get it wrong because it's predicting words, it wrong because it's predicting words, it wrong because it's predicting words, not calculating numbers. You ask about not calculating numbers. You ask about not calculating numbers. You ask about something that happened last week and it something that happened last week and it something that happened last week and it won't know. Its knowledge ends won't know. Its knowledge ends won't know. Its knowledge ends at whatever date it was last trained. at whatever date it was last trained. at whatever date it was last trained. If you ask it something that it doesn't If you ask it something that it doesn't If you ask it something that it doesn't know the answer to, know the answer to, know the answer to, instead of saying, "I'm not sure," it instead of saying, "I'm not sure," it instead of saying, "I'm not sure," it often makes up a confident-sounding often makes up a confident-sounding often makes up a confident-sounding answer. And this is called hallucination answer. And this is called hallucination answer. And this is called hallucination and it's one of the biggest challenges and it's one of the biggest challenges and it's one of the biggest challenges with LLMs. with LLMs. with LLMs. The biggest limitation though is the one The biggest limitation though is the one The biggest limitation though is the one that matters most for this course. that matters most for this course. that matters most for this course. On their own, LLMs can only produce On their own, LLMs can only produce On their own, LLMs can only produce text. They can't look up a database, text. They can't look up a database, text. They can't look up a database, send an email, book a meeting, or take send an email, book a meeting, or take send an email, book a meeting, or take any action in the real world. An LLM by any action in the real world. An LLM by any action in the real world. An LLM by itself is just a very impressive text itself is just a very impressive text itself is just a very impressive text generator. generator. generator. When we build Zippy later, we'll give When we build Zippy later, we'll give When we build Zippy later, we'll give him tools to overcome the limitations of him tools to overcome the limitations of him tools to overcome the limitations of a raw LLM. a raw LLM. a raw LLM. So, give it tools, the ability to So, give it tools, the ability to So, give it tools, the ability to search, calculate, and take actions and search, calculate, and take actions and search, calculate, and take actions and LLMs start to become an AI agent. LLMs start to become an AI agent. LLMs start to become an AI agent. Let us now see how developers use LLMs. Let us now see how developers use LLMs. Let us now see how developers use LLMs. When you use ChatGPT, you type in a text When you use ChatGPT, you type in a text When you use ChatGPT, you type in a text box and get a response back in the chat. box and get a response back in the chat. box and get a response back in the chat. When a developer builds with an LLM, When a developer builds with an LLM, When a developer builds with an LLM, they use an API, an application they use an API, an application they use an API, an application programming interface, a way for one programming interface, a way for one programming interface, a way for one program to talk to another. program to talk to another. program to talk to another. And here's what that looks like. Your And here's what that looks like. Your And here's what that looks like. Your application sends a request to the LLM application sends a request to the LLM application sends a request to the LLM provider, something like summarize this provider, something like summarize this provider, something like summarize this text. text. text. The provider processes it through the The provider processes it through the The provider processes it through the model and sends a response back. It's a model and sends a response back. It's a model and sends a response back. It's a simple back and forth, request in, simple back and forth, request in, simple back and forth, request in, request out. request out. request out. Every AI agent is built using an API. Every AI agent is built using an API. Every AI agent is built using an API. The agent sends messages to the LLM, The agent sends messages to the LLM, The agent sends messages to the LLM, gets responses back, and decide what to gets responses back, and decide what to gets responses back, and decide what to do next. LLMs don't read text the way you do. LLMs don't read text the way you do. LLMs don't read text the way you do. You see words and LLM sees tokens. You see words and LLM sees tokens. You see words and LLM sees tokens. A token is a chunk of text, sometimes a A token is a chunk of text, sometimes a A token is a chunk of text, sometimes a whole word, sometimes part of a word, whole word, sometimes part of a word, whole word, sometimes part of a word, sometimes just a single character. sometimes just a single character. sometimes just a single character. Before an LLM processes anything you Before an LLM processes anything you Before an LLM processes anything you type, it first breaks your text into type, it first breaks your text into type, it first breaks your text into these tokens. these tokens. these tokens. This process is called tokenization. This process is called tokenization. This process is called tokenization. Understanding tokens matter because they Understanding tokens matter because they Understanding tokens matter because they affect almost everything, how much a affect almost everything, how much a affect almost everything, how much a request costs, how much text you can request costs, how much text you can request costs, how much text you can send and receive, and why an LLM send and receive, and why an LLM send and receive, and why an LLM sometimes splits a word in a strange sometimes splits a word in a strange sometimes splits a word in a strange place. place. place. When you're building AI agents, tokens When you're building AI agents, tokens When you're building AI agents, tokens are the currency you're spending with are the currency you're spending with are the currency you're spending with every single interaction. every single interaction. every single interaction. So, here's a concrete example. Take the So, here's a concrete example. Take the So, here's a concrete example. Take the sentence, "The tokenization process is sentence, "The tokenization process is sentence, "The tokenization process is fascinating." fascinating." fascinating." When tokenized, it breaks it into six When tokenized, it breaks it into six When tokenized, it breaks it into six tokens. tokens. tokens. The The The tokenization process tokenization process tokenization process is fascinating. Most of those words is fascinating. Most of those words is fascinating. Most of those words stayed whole. They're common enough to stayed whole. They're common enough to stayed whole. They're common enough to be their own token. But tokenization got be their own token. But tokenization got be their own token. But tokenization got split into two, token and ization. So, split into two, token and ization. So, split into two, token and ization. So, why did tokenization get split? Because why did tokenization get split? Because why did tokenization get split? Because the tokenizer learned that token and the tokenizer learned that token and the tokenizer learned that token and ization are common building blocks. By ization are common building blocks. By ization are common building blocks. By breaking words into reusable pieces, the breaking words into reusable pieces, the breaking words into reusable pieces, the LLM can handle words it's never seen LLM can handle words it's never seen LLM can handle words it's never seen before. So, if it knows token and before. So, if it knows token and before. So, if it knows token and ization separately, it can also ization separately, it can also ization separately, it can also understand globalization, understand globalization, understand globalization, visualization, visualization, visualization, civilization, any word with that ending. civilization, any word with that ending. civilization, any word with that ending. The rarer the word, the more pieces it The rarer the word, the more pieces it The rarer the word, the more pieces it gets broken into. gets broken into. gets broken into. A word like anthropomorphization, A word like anthropomorphization, A word like anthropomorphization, I hope I got that right, becomes five I hope I got that right, becomes five I hope I got that right, becomes five separate tokens. separate tokens. separate tokens. Not every word is one token. Not every word is one token. Not every word is one token. Common short words like the, is, and and Common short words like the, is, and and Common short words like the, is, and and are each one token. They're extremely are each one token. They're extremely are each one token. They're extremely frequent in training data. Common longer frequent in training data. Common longer frequent in training data. Common longer words like computer and language also words like computer and language also words like computer and language also get their own single token because they get their own single token because they get their own single token because they appear often enough. Uncommon words like appear often enough. Uncommon words like appear often enough. Uncommon words like tokenization get split into two or three tokenization get split into two or three tokenization get split into two or three tokens, broken into known sub-pieces. tokens, broken into known sub-pieces. tokens, broken into known sub-pieces. Numbers vary by tokenizer and might be Numbers vary by tokenizer and might be Numbers vary by tokenizer and might be one or two tokens. Code tends to use one or two tokens. Code tends to use one or two tokens. Code tends to use more tokens because punctuation and more tokens because punctuation and more tokens because punctuation and keywords get split up and non-English keywords get split up and non-English keywords get split up and non-English text typically uses more tokens per word text typically uses more tokens per word text typically uses more tokens per word because it because it because it was less represented in the training was less represented in the training was less represented in the training data. data. data. The pattern here is frequency. The more The pattern here is frequency. The more The pattern here is frequency. The more often a word or phrase appeared in the often a word or phrase appeared in the often a word or phrase appeared in the training data, the more likely it gets training data, the more likely it gets training data, the more likely it gets its own single token. Rare words, its own single token. Rare words, its own single token. Rare words, technical jargons, and non-English text technical jargons, and non-English text technical jargons, and non-English text get broken into smaller pieces, which get broken into smaller pieces, which get broken into smaller pieces, which means they cost more tokens and take means they cost more tokens and take means they cost more tokens and take slightly more effort for the model to slightly more effort for the model to slightly more effort for the model to process. Now, a rough rule of thumb, one process. Now, a rough rule of thumb, one process. Now, a rough rule of thumb, one token is roughly four characters of token is roughly four characters of token is roughly four characters of English text or about three quarters of English text or about three quarters of English text or about three quarters of a word. a word. a word. So, a 1,000-word essay is roughly 1,300 So, a 1,000-word essay is roughly 1,300 So, a 1,000-word essay is roughly 1,300 to 1,500 tokens. Tokens aren't just a to 1,500 tokens. Tokens aren't just a to 1,500 tokens. Tokens aren't just a technical detail, they directly affect technical detail, they directly affect technical detail, they directly affect three things you'll care about when three things you'll care about when three things you'll care about when working with LLMs, cost, speed, and the working with LLMs, cost, speed, and the working with LLMs, cost, speed, and the context window. context window. context window. Starting with cost, LLM providers like Starting with cost, LLM providers like Starting with cost, LLM providers like OpenAI, Anthropic, and Google charge per OpenAI, Anthropic, and Google charge per OpenAI, Anthropic, and Google charge per token both for what you send in and what token both for what you send in and what token both for what you send in and what you get back. More token means more you get back. More token means more you get back. More token means more money. To give you a real example, money. To give you a real example, money. To give you a real example, GPT-4o charges around $2.50 GPT-4o charges around $2.50 GPT-4o charges around $2.50 per million input tokens and $10 per per million input tokens and $10 per per million input tokens and $10 per million output tokens. A 500-word prompt million output tokens. A 500-word prompt million output tokens. A 500-word prompt costs fractions of a cent, but here's costs fractions of a cent, but here's costs fractions of a cent, but here's the thing, output tokens are four times the thing, output tokens are four times the thing, output tokens are four times more expensive than input tokens. That's more expensive than input tokens. That's more expensive than input tokens. That's because input tokens are processed all because input tokens are processed all because input tokens are processed all at once in parallel, while output tokens at once in parallel, while output tokens at once in parallel, while output tokens are generated one at a time, each are generated one at a time, each are generated one at a time, each requiring a full pass through the model. requiring a full pass through the model. requiring a full pass through the model. That sequential generation is much more That sequential generation is much more That sequential generation is much more compute-intensive. compute-intensive. compute-intensive. Now, this seems cheap for a single Now, this seems cheap for a single Now, this seems cheap for a single request, but an AI agent that makes request, but an AI agent that makes request, but an AI agent that makes hundreds of LLM calls per task can add hundreds of LLM calls per task can add hundreds of LLM calls per task can add up fast. up fast. up fast. Speed is the second factor. LLMs Speed is the second factor. LLMs Speed is the second factor. LLMs generate one token at a time. It's the generate one token at a time. It's the generate one token at a time. It's the next word prediction we covered earlier. next word prediction we covered earlier. next word prediction we covered earlier. Now, more tokens in the response means a Now, more tokens in the response means a Now, more tokens in the response means a longer wait. A 100-token response comes longer wait. A 100-token response comes longer wait. A 100-token response comes back much faster than a 2,000-token back much faster than a 2,000-token back much faster than a 2,000-token response. And third, the context window. response. And third, the context window. response. And third, the context window. Every LLM has a maximum number of tokens Every LLM has a maximum number of tokens Every LLM has a maximum number of tokens it can handle at once. That's the it can handle at once. That's the it can handle at once. That's the context window. It includes both your context window. It includes both your context window. It includes both your input and the model's output. To give input and the model's output. To give input and the model's output. To give you a sense of how fast this has grown, you a sense of how fast this has grown, you a sense of how fast this has grown, GPT-3.5 in 2022 had a context window of GPT-3.5 in 2022 had a context window of GPT-3.5 in 2022 had a context window of about 4,000 tokens. GPT-4o in 2024 about 4,000 tokens. GPT-4o in 2024 about 4,000 tokens. GPT-4o in 2024 jumped to 128,000 and now in 2026 models jumped to 128,000 and now in 2026 models jumped to 128,000 and now in 2026 models like Claude Opus 4.6, GPT-5.5, and like Claude Opus 4.6, GPT-5.5, and like Claude Opus 4.6, GPT-5.5, and Gemini 2.5 all support up to a million Gemini 2.5 all support up to a million Gemini 2.5 all support up to a million tokens. tokens. tokens. That's roughly 750,000 words. Llama 4 That's roughly 750,000 words. Llama 4 That's roughly 750,000 words. Llama 4 Scout goes even further at 10 million Scout goes even further at 10 million Scout goes even further at 10 million tokens. So, if your input and output tokens. So, if your input and output tokens. So, if your input and output together exceed the context window, the together exceed the context window, the together exceed the context window, the model can't process it. It's a hard model can't process it. It's a hard model can't process it. It's a hard limit, not a suggestion. We'll cover limit, not a suggestion. We'll cover limit, not a suggestion. We'll cover context windows in more depth in the context windows in more depth in the context windows in more depth in the upcoming lessons. One more thing worth upcoming lessons. One more thing worth upcoming lessons. One more thing worth knowing, different LLMs use different knowing, different LLMs use different knowing, different LLMs use different tokenizers. The same sentence might be tokenizers. The same sentence might be tokenizers. The same sentence might be fit GPT-4 and 18 tokens in Llama 3. Each fit GPT-4 and 18 tokens in Llama 3. Each fit GPT-4 and 18 tokens in Llama 3. Each company trains their own tokenizer on company trains their own tokenizer on company trains their own tokenizer on their own data, so the exact splits their own data, so the exact splits their own data, so the exact splits vary. This means token counts are always vary. This means token counts are always vary. This means token counts are always approximate when comparing across approximate when comparing across approximate when comparing across models. models. models. Now, if you want to see this in action, Now, if you want to see this in action, Now, if you want to see this in action, OpenAI provides a free tool called the OpenAI provides a free tool called the OpenAI provides a free tool called the tokenizer at tokenizer at tokenizer at platform.openai.com/tokenizer. You can paste any text and see exactly You can paste any text and see exactly You can paste any text and see exactly how it gets split into tokens. how it gets split into tokens. how it gets split into tokens. Try pasting a paragraph and notice which Try pasting a paragraph and notice which Try pasting a paragraph and notice which words stay whole and which get broken. You've asked ChatGPT the same question You've asked ChatGPT the same question You've asked ChatGPT the same question twice and gotten different answers. Why? twice and gotten different answers. Why? twice and gotten different answers. Why? LLMs LLMs LLMs don't just pick the single most likely don't just pick the single most likely don't just pick the single most likely response every time. They calculate a response every time. They calculate a response every time. They calculate a probability for every possible probability for every possible probability for every possible completion, then sample from that completion, then sample from that completion, then sample from that distribution. Take a prompt like, "Write distribution. Take a prompt like, "Write distribution. Take a prompt like, "Write a one-sentence story about a robot." The a one-sentence story about a robot." The a one-sentence story about a robot." The model might give, "A lonely robot model might give, "A lonely robot model might give, "A lonely robot discovered a forgotten garden." a 30% discovered a forgotten garden." a 30% discovered a forgotten garden." a 30% chance. "In a world where emotions were chance. "In a world where emotions were chance. "In a world where emotions were outlawed." gets 15% and thousands of outlawed." gets 15% and thousands of outlawed." gets 15% and thousands of other stories get smaller slices. The other stories get smaller slices. The other stories get smaller slices. The model rolls the dice. Responses with model rolls the dice. Responses with model rolls the dice. Responses with higher probability are more likely to higher probability are more likely to higher probability are more likely to win, but they don't always win. So, now win, but they don't always win. So, now win, but they don't always win. So, now we know LLMs are choosing from a list of we know LLMs are choosing from a list of we know LLMs are choosing from a list of possibilities. But, here's the natural possibilities. But, here's the natural possibilities. But, here's the natural question. How risky are those choices? question. How risky are those choices? question. How risky are those choices? Does the model always play it safe and Does the model always play it safe and Does the model always play it safe and pick the top response, or does it pick the top response, or does it pick the top response, or does it sometimes go out on a limb? sometimes go out on a limb? sometimes go out on a limb? That's exactly what temperature That's exactly what temperature That's exactly what temperature controls. Temperature is a number controls. Temperature is a number controls. Temperature is a number between 0 and 2 that you set when you between 0 and 2 that you set when you between 0 and 2 that you set when you call the model. Think of it like a dial call the model. Think of it like a dial call the model. Think of it like a dial that goes from strictly by the book to that goes from strictly by the book to that goes from strictly by the book to wildly creative. At temperature 0, the wildly creative. At temperature 0, the wildly creative. At temperature 0, the model always picks the most likely model always picks the most likely model always picks the most likely response. No randomness at all. Uh same response. No randomness at all. Uh same response. No randomness at all. Uh same input, same output every time. input, same output every time. input, same output every time. At 0.5 to 0.7, it mostly picks likely At 0.5 to 0.7, it mostly picks likely At 0.5 to 0.7, it mostly picks likely responses, but occasionally surprises responses, but occasionally surprises responses, but occasionally surprises you. Crank it up past 1.0 and the model you. Crank it up past 1.0 and the model you. Crank it up past 1.0 and the model starts spreading its bets more evenly starts spreading its bets more evenly starts spreading its bets more evenly across all options, which gets creative across all options, which gets creative across all options, which gets creative and sometimes nonsensical. Here's a and sometimes nonsensical. Here's a and sometimes nonsensical. Here's a simple way to see this in action. simple way to see this in action. simple way to see this in action. Take our robot prompt and call the model Take our robot prompt and call the model Take our robot prompt and call the model 10 times at temperature 0, then 10 times 10 times at temperature 0, then 10 times 10 times at temperature 0, then 10 times at temperature 1.5. On the left, at temperature 1.5. On the left, at temperature 1.5. On the left, temperature 0, you get the same story temperature 0, you get the same story temperature 0, you get the same story every single run. every single run. every single run. "A lonely robot discovered a forgotten "A lonely robot discovered a forgotten "A lonely robot discovered a forgotten garden." again and again. On the right, garden." again and again. On the right, garden." again and again. On the right, temperature 1.5, every run is different. temperature 1.5, every run is different. temperature 1.5, every run is different. A robot building friends from junkyard A robot building friends from junkyard A robot building friends from junkyard parts, a robot learning to dream, a parts, a robot learning to dream, a parts, a robot learning to dream, a robot discovering emotions. Same prompt, robot discovering emotions. Same prompt, robot discovering emotions. Same prompt, completely different behavior, completely different behavior, completely different behavior, controlled by a single number. controlled by a single number. controlled by a single number. So, when should you use each So, when should you use each So, when should you use each temperature? temperature? temperature? For data extraction and classification, For data extraction and classification, For data extraction and classification, use 0. use 0. use 0. You want the same answer every time. For You want the same answer every time. For You want the same answer every time. For code generation, 0 to 0.2. Code needs to code generation, 0 to 0.2. Code needs to code generation, 0 to 0.2. Code needs to be correct, not creative. General be correct, not creative. General be correct, not creative. General conversation, 0.5 to 0.7. Natural, but conversation, 0.5 to 0.7. Natural, but conversation, 0.5 to 0.7. Natural, but reliable. Creative writing, 0.8 to 1.2. reliable. Creative writing, 0.8 to 1.2. reliable. Creative writing, 0.8 to 1.2. You want variety and surprise. For AI You want variety and surprise. For AI You want variety and surprise. For AI agents, lower is generally better. You agents, lower is generally better. You agents, lower is generally better. You want reliable decisions, not creative want reliable decisions, not creative want reliable decisions, not creative ones. Temperature is set per API call. ones. Temperature is set per API call. ones. Temperature is set per API call. You control it, not the model. The context window is the LLM's working The context window is the LLM's working The context window is the LLM's working memory. It's the total amount of text memory. It's the total amount of text memory. It's the total amount of text the model can see at any given moment. the model can see at any given moment. the model can see at any given moment. Your input, the conversation history, Your input, the conversation history, Your input, the conversation history, and the model's own output all combined. and the model's own output all combined. and the model's own output all combined. Here's what it looks like in practice. Here's what it looks like in practice. Here's what it looks like in practice. Everything gets packed into one window. Everything gets packed into one window. Everything gets packed into one window. The system instructions, the The system instructions, the The system instructions, the conversation so far, your latest conversation so far, your latest conversation so far, your latest message, and whatever the model has message, and whatever the model has message, and whatever the model has generated. In a typical interaction, you generated. In a typical interaction, you generated. In a typical interaction, you might have 200 tokens of system might have 200 tokens of system might have 200 tokens of system instructions, 3,000 tokens of instructions, 3,000 tokens of instructions, 3,000 tokens of conversation history, 150 tokens for conversation history, 150 tokens for conversation history, 150 tokens for your latest message, and 500 tokens for your latest message, and 500 tokens for your latest message, and 500 tokens for the model's response so far. That's the model's response so far. That's the model's response so far. That's about 3,150 tokens total. If your model about 3,150 tokens total. If your model about 3,150 tokens total. If your model has a 128,000 token context window, you has a 128,000 token context window, you has a 128,000 token context window, you still have plenty of room. But, still have plenty of room. But, still have plenty of room. But, everything inside the window the model everything inside the window the model everything inside the window the model can see. Everything outside it doesn't can see. Everything outside it doesn't can see. Everything outside it doesn't exist to the model. This is a hard exist to the model. This is a hard exist to the model. This is a hard limit, not a suggestion. Different limit, not a suggestion. Different limit, not a suggestion. Different models have very different window sizes. models have very different window sizes. models have very different window sizes. GPT-3.5 had just 4,096 tokens, roughly GPT-3.5 had just 4,096 tokens, roughly GPT-3.5 had just 4,096 tokens, roughly six pages of text. GPT-4 doubled that to six pages of text. GPT-4 doubled that to six pages of text. GPT-4 doubled that to about 12 pages. GPT-4o expanded to about 12 pages. GPT-4o expanded to about 12 pages. GPT-4o expanded to 128,000 tokens, which is roughly 200 128,000 tokens, which is roughly 200 128,000 tokens, which is roughly 200 pages, about the length of a short pages, about the length of a short pages, about the length of a short novel. Claude 3.5 Sonnet goes up to novel. Claude 3.5 Sonnet goes up to novel. Claude 3.5 Sonnet goes up to 200,000 tokens, around 300 pages. And 200,000 tokens, around 300 pages. And 200,000 tokens, around 300 pages. And Gemini 1.5 Pro reaches 1 million tokens, Gemini 1.5 Pro reaches 1 million tokens, Gemini 1.5 Pro reaches 1 million tokens, about 1,500 pages. about 1,500 pages. about 1,500 pages. Larger context windows have been one of Larger context windows have been one of Larger context windows have been one of the biggest competitive battles between the biggest competitive battles between the biggest competitive battles between LLM providers. A bigger window means the LLM providers. A bigger window means the LLM providers. A bigger window means the model can process longer documents, model can process longer documents, model can process longer documents, remember more conversation history, and remember more conversation history, and remember more conversation history, and handle more complex tasks. So, what handle more complex tasks. So, what handle more complex tasks. So, what happens when you hit the limit? happens when you hit the limit? happens when you hit the limit? When the conversation exceeds the When the conversation exceeds the When the conversation exceeds the context window, something has to go. context window, something has to go. context window, something has to go. Different systems handle this Different systems handle this Different systems handle this differently. differently. differently. The most common approach is truncation. The most common approach is truncation. The most common approach is truncation. The oldest messages get dropped. The The oldest messages get dropped. The The oldest messages get dropped. The model loses the beginning of the model loses the beginning of the model loses the beginning of the conversation and only sees the most conversation and only sees the most conversation and only sees the most recent messages. recent messages. recent messages. Another approach is summarization. The Another approach is summarization. The Another approach is summarization. The system asks the LLM to summarize older system asks the LLM to summarize older system asks the LLM to summarize older messages, then replaces the full history messages, then replaces the full history messages, then replaces the full history with that summary. This preserves key with that summary. This preserves key with that summary. This preserves key information in fewer tokens. And there's information in fewer tokens. And there's information in fewer tokens. And there's the sliding window approach. A the sliding window approach. A the sliding window approach. A fixed-size window moves forward through fixed-size window moves forward through fixed-size window moves forward through the conversation, always keeping the the conversation, always keeping the the conversation, always keeping the most um recent content. most um recent content. most um recent content. Let's see what uh Let's see what uh Let's see what uh truncation looks like in practice. truncation looks like in practice. truncation looks like in practice. Imagine you've been chatting with an Imagine you've been chatting with an Imagine you've been chatting with an assistant. Early on, you told it your assistant. Early on, you told it your assistant. Early on, you told it your name is Alex, that you prefer afternoon name is Alex, that you prefer afternoon name is Alex, that you prefer afternoon flights, and that you're vegetarian. flights, and that you're vegetarian. flights, and that you're vegetarian. But, as the conversation grows past the But, as the conversation grows past the But, as the conversation grows past the context window, those early messages get context window, those early messages get context window, those early messages get dropped. The model can only see the most dropped. The model can only see the most dropped. The model can only see the most recent turns. It no longer knows your recent turns. It no longer knows your recent turns. It no longer knows your name or your preferences. This is why name or your preferences. This is why name or your preferences. This is why ChatGPT sometimes forgets things you ChatGPT sometimes forgets things you ChatGPT sometimes forgets things you told it earlier in a long conversation. told it earlier in a long conversation. told it earlier in a long conversation. It didn't actually forget. Those It didn't actually forget. Those It didn't actually forget. Those messages fell outside the context messages fell outside the context messages fell outside the context window, and this brings up a critical window, and this brings up a critical window, and this brings up a critical distinction. The context window is not distinction. The context window is not distinction. The context window is not long-term memory. It's more like a long-term memory. It's more like a long-term memory. It's more like a whiteboard. Everything written on the whiteboard. Everything written on the whiteboard. Everything written on the whiteboard right now is the context whiteboard right now is the context whiteboard right now is the context window. window. window. When it fills up, you erase the oldest When it fills up, you erase the oldest When it fills up, you erase the oldest stuff. Long-term memory, by contrast, is stuff. Long-term memory, by contrast, is stuff. Long-term memory, by contrast, is like a notebook you can refer back to like a notebook you can refer back to like a notebook you can refer back to anytime. LLMs don't have this natively. anytime. LLMs don't have this natively. anytime. LLMs don't have this natively. Some products, like ChatGPT's memory Some products, like ChatGPT's memory Some products, like ChatGPT's memory feature, simulate long-term memory by feature, simulate long-term memory by feature, simulate long-term memory by storing key facts separately and storing key facts separately and storing key facts separately and injecting them into the context window injecting them into the context window injecting them into the context window on each request. But, the underlying on each request. But, the underlying on each request. But, the underlying model still only sees what's in the model still only sees what's in the model still only sees what's in the window. For a simple chatbot, running window. For a simple chatbot, running window. For a simple chatbot, running out of context window just means the out of context window just means the out of context window just means the model forgets earlier conversation. model forgets earlier conversation. model forgets earlier conversation. Annoying, but not critical. For an AI Annoying, but not critical. For an AI Annoying, but not critical. For an AI agent, it's a much bigger deal. An agent agent, it's a much bigger deal. An agent agent, it's a much bigger deal. An agent might need to hold all of this in might need to hold all of this in might need to hold all of this in context at once. System instructions for context at once. System instructions for context at once. System instructions for how to behave, tool definitions for what how to behave, tool definitions for what how to behave, tool definitions for what tools are available, conversation tools are available, conversation tools are available, conversation history from the user, results returned history from the user, results returned history from the user, results returned from tool calls, and the agent's own from tool calls, and the agent's own from tool calls, and the agent's own intermediate reasoning. Here's a rough intermediate reasoning. Here's a rough intermediate reasoning. Here's a rough breakdown of how those tokens get used breakdown of how those tokens get used breakdown of how those tokens get used in a single turn. The system prompt in a single turn. The system prompt in a single turn. The system prompt takes about 2,000 tokens. Tool takes about 2,000 tokens. Tool takes about 2,000 tokens. Tool definitions for 10 tools eat up another definitions for 10 tools eat up another definitions for 10 tools eat up another 3,000. 3,000. 3,000. Add in conversation history, tool Add in conversation history, tool Add in conversation history, tool results, the agent's own reasoning, and results, the agent's own reasoning, and results, the agent's own reasoning, and space for generating a response, and space for generating a response, and space for generating a response, and you're already at around 23,000 tokens you're already at around 23,000 tokens you're already at around 23,000 tokens for just one turn. Each turn of the for just one turn. Each turn of the for just one turn. Each turn of the agent loop consumes context. A complex agent loop consumes context. A complex agent loop consumes context. A complex task with many tool calls can eat task with many tool calls can eat task with many tool calls can eat through the window fast. This is why through the window fast. This is why through the window fast. This is why context window management is a real context window management is a real context window management is a real engineering challenge when building engineering challenge when building engineering challenge when building agents, and why models with larger agents, and why models with larger agents, and why models with larger windows are so valuable. And bigger windows are so valuable. And bigger windows are so valuable. And bigger context windows aren't free. Remember, context windows aren't free. Remember, context windows aren't free. Remember, you pay per token. If you stuff 100,000 you pay per token. If you stuff 100,000 you pay per token. If you stuff 100,000 tokens into the context window, you're tokens into the context window, you're tokens into the context window, you're paying for all of those tokens on every paying for all of those tokens on every paying for all of those tokens on every single API call. An agent that makes 10 single API call. An agent that makes 10 single API call. An agent that makes 10 API calls with 50,000 tokens of context API calls with 50,000 tokens of context API calls with 50,000 tokens of context each time is paying for 500,000 input each time is paying for 500,000 input each time is paying for 500,000 input tokens, even if most of that context is tokens, even if most of that context is tokens, even if most of that context is the same conversation history repeated. the same conversation history repeated. the same conversation history repeated. This creates a direct tension. More This creates a direct tension. More This creates a direct tension. More context means better results, but higher context means better results, but higher context means better results, but higher cost. Managing this trade-off is one of cost. Managing this trade-off is one of cost. Managing this trade-off is one of the key skills in building production AI the key skills in building production AI the key skills in building production AI agents. You've learned that the context window You've learned that the context window You've learned that the context window is the model's working memory, is the model's working memory, is the model's working memory, everything it can see at once. everything it can see at once. everything it can see at once. Now, the question is, what do you Now, the question is, what do you Now, the question is, what do you actually put in that window? Because actually put in that window? Because actually put in that window? Because what you give the model completely what you give the model completely what you give the model completely determines what you get back. Most determines what you get back. Most determines what you get back. Most people assume prompting is just about people assume prompting is just about people assume prompting is just about being specific. Give clear instructions, being specific. Give clear instructions, being specific. Give clear instructions, add some context, and you're done. add some context, and you're done. add some context, and you're done. That's what I thought, too. That's what I thought, too. That's what I thought, too. But, there's something strange going on But, there's something strange going on But, there's something strange going on under the hood. Here's an example that under the hood. Here's an example that under the hood. Here's an example that surprised me. These two instructions surprised me. These two instructions surprised me. These two instructions mean exactly the same thing. "Don't use mean exactly the same thing. "Don't use mean exactly the same thing. "Don't use bullet points." bullet points." bullet points." "Write in paragraphs only." "Write in paragraphs only." "Write in paragraphs only." Same meaning, but the second one works Same meaning, but the second one works Same meaning, but the second one works better, measurably, consistently better. better, measurably, consistently better. better, measurably, consistently better. Why? When the model reads, "Don't use Why? When the model reads, "Don't use Why? When the model reads, "Don't use bullet points." it first activates the bullet points." it first activates the bullet points." it first activates the bullet point pattern, because that's the bullet point pattern, because that's the bullet point pattern, because that's the strongest signal word in the sentence, strongest signal word in the sentence, strongest signal word in the sentence, and then tries to suppress it. The and then tries to suppress it. The and then tries to suppress it. The suppression doesn't always win. You told suppression doesn't always win. You told suppression doesn't always win. You told it not to, but you also put the idea it not to, but you also put the idea it not to, but you also put the idea there. Prompting isn't just writing there. Prompting isn't just writing there. Prompting isn't just writing instructions. It's shaping which instructions. It's shaping which instructions. It's shaping which patterns in the model get activated. And patterns in the model get activated. And patterns in the model get activated. And once you understand that, the whole once you understand that, the whole once you understand that, the whole thing gets a lot more interesting. When thing gets a lot more interesting. When thing gets a lot more interesting. When you use the API, messages aren't just you use the API, messages aren't just you use the API, messages aren't just plain text. Every message has a role plain text. Every message has a role plain text. Every message has a role attached to it. There are three roles. attached to it. There are three roles. attached to it. There are three roles. The system role is for the developer. The system role is for the developer. The system role is for the developer. It sets the behavior, rules, and It sets the behavior, rules, and It sets the behavior, rules, and personality of the model. The user role personality of the model. The user role personality of the model. The user role is the actual question or request from is the actual question or request from is the actual question or request from the person using the app. And the the person using the app. And the the person using the app. And the assistant role is the model's response. assistant role is the model's response. assistant role is the model's response. Look at this messages array. Look at this messages array. Look at this messages array. The first item has role system. The first item has role system. The first item has role system. That's the developer talking. The That's the developer talking. The That's the developer talking. The content says, "You are a personal content says, "You are a personal content says, "You are a personal assistant that helps manage schedules assistant that helps manage schedules assistant that helps manage schedules and tasks." and tasks." and tasks." That's the instruction that shapes every That's the instruction that shapes every That's the instruction that shapes every single reply. single reply. single reply. The second item has role The second item has role The second item has role user. That's the person asking, "What's user. That's the person asking, "What's user. That's the person asking, "What's on my calendar today?" on my calendar today?" on my calendar today?" The system message is invisible to the The system message is invisible to the The system message is invisible to the user, but it's always there, always in user, but it's always there, always in user, but it's always there, always in context. In ChatGPT, OpenAI writes it context. In ChatGPT, OpenAI writes it context. In ChatGPT, OpenAI writes it for you. When you build your own app, for you. When you build your own app, for you. When you build your own app, you write it yourself. That's a big part you write it yourself. That's a big part you write it yourself. That's a big part of what makes the API more powerful than of what makes the API more powerful than of what makes the API more powerful than chatting. Let's make this concrete. The chatting. Let's make this concrete. The chatting. Let's make this concrete. The same question with three different same question with three different same question with three different prompts produces three completely prompts produces three completely prompts produces three completely different results. If you say, "Help me different results. If you say, "Help me different results. If you say, "Help me with my schedule." you get vague, with my schedule." you get vague, with my schedule." you get vague, generic advice about time management. generic advice about time management. generic advice about time management. Not very useful. If you say, "List my Not very useful. If you say, "List my Not very useful. If you say, "List my meetings for today in bullet points with meetings for today in bullet points with meetings for today in bullet points with times." times." times." Now, the model knows the format you want Now, the model knows the format you want Now, the model knows the format you want and the information to include. Short, and the information to include. Short, and the information to include. Short, structured, useful. structured, useful. structured, useful. Go further. Go further. Go further. "You are a personal assistant. The user "You are a personal assistant. The user "You are a personal assistant. The user has a busy day. Summarize their has a busy day. Summarize their has a busy day. Summarize their calendar, flag any conflicts, and calendar, flag any conflicts, and calendar, flag any conflicts, and suggest which meeting to reschedule if suggest which meeting to reschedule if suggest which meeting to reschedule if they're double booked." And now you get they're double booked." And now you get they're double booked." And now you get something specific, practical, and something specific, practical, and something specific, practical, and actionable. More context, more actionable. More context, more actionable. More context, more constraints, more useful output. Instead constraints, more useful output. Instead constraints, more useful output. Instead of, "Summarize this." of, "Summarize this." of, "Summarize this." say, "Summarize this in three bullet say, "Summarize this in three bullet say, "Summarize this in three bullet points for a technical audience." Every points for a technical audience." Every points for a technical audience." Every word you add to a prompt is a word you add to a prompt is a word you add to a prompt is a constraint, and constraints produce constraint, and constraints produce constraint, and constraints produce better output. This one is powerful. You better output. This one is powerful. You better output. This one is powerful. You can show the model exactly what you want can show the model exactly what you want can show the model exactly what you want by including an example in the prompt. by including an example in the prompt. by including an example in the prompt. This is called few-shot prompting. This is called few-shot prompting. This is called few-shot prompting. And the difference it makes is dramatic. And the difference it makes is dramatic. And the difference it makes is dramatic. Here's the same task, same question, Here's the same task, same question, Here's the same task, same question, with two different prompts. Without an with two different prompts. Without an with two different prompts. Without an example, zero-shot. There are two example, zero-shot. There are two example, zero-shot. There are two messages. The system message says, messages. The system message says, messages. The system message says, "Classify the sentiment as positive, "Classify the sentiment as positive, "Classify the sentiment as positive, negative, or neutral." negative, or neutral." negative, or neutral." The user message is, "It took 2 hours to The user message is, "It took 2 hours to The user message is, "It took 2 hours to get our order." That's it. No example of get our order." That's it. No example of get our order." That's it. No example of what the answer should look like. And what the answer should look like. And what the answer should look like. And here's what the model produces. The here's what the model produces. The here's what the model produces. The sentiment of this review is negative. sentiment of this review is negative. sentiment of this review is negative. The customer experienced a long wait The customer experienced a long wait The customer experienced a long wait time, which suggests frustration and time, which suggests frustration and time, which suggests frustration and dissatisfaction with the service. You dissatisfaction with the service. You dissatisfaction with the service. You asked for a classification, you got an asked for a classification, you got an asked for a classification, you got an essay. The model had no idea you wanted essay. The model had no idea you wanted essay. The model had no idea you wanted one word. So, it gave you what it one word. So, it gave you what it one word. So, it gave you what it thought was a thorough answer. Now, with thought was a thorough answer. Now, with thought was a thorough answer. Now, with an example, few-shot. This time, there an example, few-shot. This time, there an example, few-shot. This time, there are four messages. Same system message, are four messages. Same system message, are four messages. Same system message, but then before the real question, you but then before the real question, you but then before the real question, you insert two extra messages. A user insert two extra messages. A user insert two extra messages. A user message that says, "The food was message that says, "The food was message that says, "The food was amazing." And an assistant message that amazing." And an assistant message that amazing." And an assistant message that says, "Positive." You wrote that says, "Positive." You wrote that says, "Positive." You wrote that assistant message yourself. It's not a assistant message yourself. It's not a assistant message yourself. It's not a real reply from the model. It's an real reply from the model. It's an real reply from the model. It's an example of what you want the reply to example of what you want the reply to example of what you want the reply to look like. Then comes the real question. look like. Then comes the real question. look like. Then comes the real question. "It took 2 hours to get our order." "It took 2 hours to get our order." "It took 2 hours to get our order." And the output? And the output? And the output? One word. Exactly what you wanted. That One word. Exactly what you wanted. That One word. Exactly what you wanted. That one example did two things at once. It one example did two things at once. It one example did two things at once. It showed the model what kind of input to showed the model what kind of input to showed the model what kind of input to expect, and it showed what format to expect, and it showed what format to expect, and it showed what format to reply in. Without it, the model guesses reply in. Without it, the model guesses reply in. Without it, the model guesses at the format. With it, there's no at the format. With it, there's no at the format. With it, there's no guessing. guessing. guessing. "You are a senior Python developer." "You are a senior Python developer." "You are a senior Python developer." produces different code than "You are a produces different code than "You are a produces different code than "You are a beginner-friendly coding tutor." This beginner-friendly coding tutor." This beginner-friendly coding tutor." This isn't just a tone change. When you isn't just a tone change. When you isn't just a tone change. When you assign a role, you're activating a assign a role, you're activating a assign a role, you're activating a cluster of patterns from training, the cluster of patterns from training, the cluster of patterns from training, the vocabulary, the reasoning style, the vocabulary, the reasoning style, the vocabulary, the reasoning style, the priorities people in that role had when priorities people in that role had when priorities people in that role had when they wrote the text the model learned they wrote the text the model learned they wrote the text the model learned from. from. from. The system message is the natural place The system message is the natural place The system message is the natural place to put this. We already saw the don't to put this. We already saw the don't to put this. We already saw the don't use bullet points example. Here's the use bullet points example. Here's the use bullet points example. Here's the pattern more broadly. Left column, the pattern more broadly. Left column, the pattern more broadly. Left column, the negative version. Right column, the negative version. Right column, the negative version. Right column, the positive rewrite. positive rewrite. positive rewrite. "Don't be verbose." becomes "Be concise, "Don't be verbose." becomes "Be concise, "Don't be verbose." becomes "Be concise, one sentence per point." "Avoid jargon." one sentence per point." "Avoid jargon." one sentence per point." "Avoid jargon." becomes "Use simple words a high school becomes "Use simple words a high school becomes "Use simple words a high school student would understand." Every one of student would understand." Every one of student would understand." Every one of those rewrites is more reliable than the those rewrites is more reliable than the those rewrites is more reliable than the version it replaces. The model handles version it replaces. The model handles version it replaces. The model handles positive instructions more reliably. positive instructions more reliably. positive instructions more reliably. We'll see exactly why in section four. We'll see exactly why in section four. We'll see exactly why in section four. Instead of, "Plan my trip." try this. Instead of, "Plan my trip." try this. Instead of, "Plan my trip." try this. Step one, check my calendar for the Step one, check my calendar for the Step one, check my calendar for the travel dates. Step two, search for travel dates. Step two, search for travel dates. Step two, search for flights within my budget. Step three, flights within my budget. Step three, flights within my budget. Step three, find hotels near my meeting location. find hotels near my meeting location. find hotels near my meeting location. Step four, summarize the best options. Step four, summarize the best options. Step four, summarize the best options. This is called chain-of-thought This is called chain-of-thought This is called chain-of-thought prompting, guiding the model to reason prompting, guiding the model to reason prompting, guiding the model to reason through the problem step by step rather through the problem step by step rather through the problem step by step rather than jumping to an answer. Each step than jumping to an answer. Each step than jumping to an answer. Each step conditions the next one. When the model conditions the next one. When the model conditions the next one. When the model writes out step one, that output becomes writes out step one, that output becomes writes out step one, that output becomes part of the context it uses to reason part of the context it uses to reason part of the context it uses to reason through step two. The model builds through step two. The model builds through step two. The model builds toward a better final answer instead of toward a better final answer instead of toward a better final answer instead of guessing all at once. On the left, guessing all at once. On the left, guessing all at once. On the left, ChatGPT. ChatGPT. ChatGPT. You type one message. "What's on my You type one message. "What's on my You type one message. "What's on my calendar today?" calendar today?" calendar today?" and send it. Everything else is handled and send it. Everything else is handled and send it. Everything else is handled for you. On the right, the API. You for you. On the right, the API. You for you. On the right, the API. You build the full messages array yourself. build the full messages array yourself. build the full messages array yourself. The system message goes first. The system message goes first. The system message goes first. "You are a personal assistant. Be "You are a personal assistant. Be "You are a personal assistant. Be concise." concise." concise." Then the user message with the same Then the user message with the same Then the user message with the same question. Same output, but now you're in question. Same output, but now you're in question. Same output, but now you're in control of every piece of input the control of every piece of input the control of every piece of input the model sees. Every concept from this model sees. Every concept from this model sees. Every concept from this section comes into play here. Your section comes into play here. Your section comes into play here. Your prompt consumes tokens from the context prompt consumes tokens from the context prompt consumes tokens from the context window. Temperature controls how the window. Temperature controls how the window. Temperature controls how the model samples its answer. And that model samples its answer. And that model samples its answer. And that entire messages array, system turn, user entire messages array, system turn, user entire messages array, system turn, user turns, assistant turns, all has to fit turns, assistant turns, all has to fit turns, assistant turns, all has to fit inside the context window you learned inside the context window you learned inside the context window you learned about last time. The techniques here about last time. The techniques here about last time. The techniques here will carry you through most of what you will carry you through most of what you will carry you through most of what you build. But there's a deeper layer. The build. But there's a deeper layer. The build. But there's a deeper layer. The research behind why these techniques research behind why these techniques research behind why these techniques work, why larger models can actually be work, why larger models can actually be work, why larger models can actually be worse at following negative worse at following negative worse at following negative instructions, how the order of instructions, how the order of instructions, how the order of information in a long prompt changes information in a long prompt changes information in a long prompt changes what the model pays attention to, and what the model pays attention to, and what the model pays attention to, and how a production agent like OpenClaw how a production agent like OpenClaw how a production agent like OpenClaw assembles its system prompt as a assembles its system prompt as a assembles its system prompt as a 19-section programmatically built file. 19-section programmatically built file. 19-section programmatically built file. That's what section four is for. For That's what section four is for. For That's what section four is for. For now, you have the foundation. You've spent the last few lessons You've spent the last few lessons You've spent the last few lessons learning what an LLM is, how tokens learning what an LLM is, how tokens learning what an LLM is, how tokens work, what temperature does, and how work, what temperature does, and how work, what temperature does, and how context windows limit attention. All context windows limit attention. All context windows limit attention. All theory. Now, it's time to write code. By theory. Now, it's time to write code. By theory. Now, it's time to write code. By the end of this lesson, you'll have a the end of this lesson, you'll have a the end of this lesson, you'll have a Python program that sends a message to Python program that sends a message to Python program that sends a message to an LLM and prints the response. Seven an LLM and prints the response. Seven an LLM and prints the response. Seven lines of code. Every concept from the lines of code. Every concept from the lines of code. Every concept from the previous lessons will have a concrete previous lessons will have a concrete previous lessons will have a concrete home in that code. When you use ChatGPT, home in that code. When you use ChatGPT, home in that code. When you use ChatGPT, a lot happens invisibly. Your message a lot happens invisibly. Your message a lot happens invisibly. Your message gets packaged up, sent over the internet gets packaged up, sent over the internet gets packaged up, sent over the internet to a server, routed to the right model, to a server, routed to the right model, to a server, routed to the right model, and the model generates tokens one at a and the model generates tokens one at a and the model generates tokens one at a time. Those tokens stream back and time. Those tokens stream back and time. Those tokens stream back and appear as text on your screen. When you appear as text on your screen. When you appear as text on your screen. When you write code to call an LLM, you're doing write code to call an LLM, you're doing write code to call an LLM, you're doing the same thing, but you control every the same thing, but you control every the same thing, but you control every step. The communication happens step. The communication happens step. The communication happens through an API, through an API, through an API, an application programming interface. an application programming interface. an application programming interface. Think of it like a standardized order Think of it like a standardized order Think of it like a standardized order form. You fill it out correctly, send it form. You fill it out correctly, send it form. You fill it out correctly, send it in, and the kitchen makes your food. in, and the kitchen makes your food. in, and the kitchen makes your food. OpenAI provides an API, so developers OpenAI provides an API, so developers OpenAI provides an API, so developers can use the same models that power can use the same models that power can use the same models that power ChatGPT in their own programs. ChatGPT in their own programs. ChatGPT in their own programs. To make API calls from Python, you use To make API calls from Python, you use To make API calls from Python, you use an SDK, a software development kit. It's an SDK, a software development kit. It's an SDK, a software development kit. It's a library that handles the formatting a library that handles the formatting a library that handles the formatting and communication for you, so you don't and communication for you, so you don't and communication for you, so you don't have to write raw HTTP requests. Without have to write raw HTTP requests. Without have to write raw HTTP requests. Without the SDK, talking to the API looks like the SDK, talking to the API looks like the SDK, talking to the API looks like this. You import urllib, manually build this. You import urllib, manually build this. You import urllib, manually build the HTTP request, set headers, encode the HTTP request, set headers, encode the HTTP request, set headers, encode JSON, decode the response. About 15 JSON, decode the response. About 15 JSON, decode the response. About 15 lines of boilerplate just to ask a lines of boilerplate just to ask a lines of boilerplate just to ask a question. question. question. With the SDK, three lines. Import With the SDK, three lines. Import With the SDK, three lines. Import openai. Create a client. Call openai. Create a client. Call openai. Create a client. Call chat.completions.create. chat.completions.create. chat.completions.create. The SDK handles the HTTP, the headers, The SDK handles the HTTP, the headers, The SDK handles the HTTP, the headers, the JSON encoding, and error handling the JSON encoding, and error handling the JSON encoding, and error handling for you. Same result. Far less noise. for you. Same result. Far less noise. for you. Same result. Far less noise. Let's walk through the code. Line one, Let's walk through the code. Line one, Let's walk through the code. Line one, from openai import openai. This loads from openai import openai. This loads from openai import openai. This loads the SDK. Line two, client equals openai the SDK. Line two, client equals openai the SDK. Line two, client equals openai with your API key. This creates your with your API key. This creates your with your API key. This creates your connection to the API. Set it up once at connection to the API. Set it up once at connection to the API. Set it up once at the top, then use it for every call. the top, then use it for every call. the top, then use it for every call. Lines three through six, Lines three through six, Lines three through six, client.chat.completions.create. You specify the model, gpt-4o, and a You specify the model, gpt-4o, and a You specify the model, gpt-4o, and a messages list with one user message. The messages list with one user message. The messages list with one user message. The result is stored in a variable called result is stored in a variable called result is stored in a variable called response. Last line, print response. Last line, print response. Last line, print response.choices[0].message.content. Choices zero is the first and usually Choices zero is the first and usually Choices zero is the first and usually only reply. .message.content only reply. .message.content only reply. .message.content is the actual text string. That's it, is the actual text string. That's it, is the actual text string. That's it, seven lines. Now, let's add temperature. seven lines. Now, let's add temperature. seven lines. Now, let's add temperature. Remember lesson five? Temperature Remember lesson five? Temperature Remember lesson five? Temperature controls how varied the responses are. controls how varied the responses are. controls how varied the responses are. Add temperature equals zero to the API Add temperature equals zero to the API Add temperature equals zero to the API call, and you get the same answer every call, and you get the same answer every call, and you get the same answer every time you run it. Change it to one, and every you run it. Change it to one, and every you run it. Change it to one, and every run gives a different answer. This is run gives a different answer. This is run gives a different answer. This is the theory from lesson five running live the theory from lesson five running live the theory from lesson five running live in your concept maps to something in the in your concept maps to something in the in your concept maps to something in the code. The LLM generating text, that's code. The LLM generating text, that's code. The LLM generating text, that's response.choices[0].message.content. Tokens, every word in your messages list Tokens, every word in your messages list Tokens, every word in your messages list is tokenized before being sent. is tokenized before being sent. is tokenized before being sent. Temperature, it's a parameter you set. Temperature, it's a parameter you set. Temperature, it's a parameter you set. Context window, everything in the Context window, everything in the Context window, everything in the messages list counts against it. messages list counts against it. messages list counts against it. Prompts, that's the content field in Prompts, that's the content field in Prompts, that's the content field in your message dictionary. Nothing in the your message dictionary. Nothing in the your message dictionary. Nothing in the code is magic. Every piece maps to code is magic. Every piece maps to code is magic. Every piece maps to something you already understand. something you already understand. something you already understand. All right. So, that was a lot of theory. All right. So, that was a lot of theory. All right. So, that was a lot of theory. And now, it's time to get our hands And now, it's time to get our hands And now, it's time to get our hands dirty. dirty. dirty. &gt;&gt; [snorts] &gt;&gt; [snorts] &gt;&gt; [snorts] &gt;&gt; Use the link in the description below to &gt;&gt; Use the link in the description below to &gt;&gt; Use the link in the description below to go to the labs enrollment page. go to the labs enrollment page. go to the labs enrollment page. The labs for the first half of this The labs for the first half of this The labs for the first half of this course come free, so you don't have to course come free, so you don't have to course come free, so you don't have to pay anything. So, you create a free pay anything. So, you create a free pay anything. So, you create a free account and sign up. And once in, you'll account and sign up. And once in, you'll account and sign up. And once in, you'll see the labs listed. Access them to get see the labs listed. Access them to get see the labs listed. Access them to get started. started. started. The lab environment already has the The lab environment already has the The lab environment already has the required API keys baked in, so you don't required API keys baked in, so you don't required API keys baked in, so you don't need to generate them from elsewhere. need to generate them from elsewhere. need to generate them from elsewhere. And these are valid for the duration of And these are valid for the duration of And these are valid for the duration of the lab. If you need [snorts] keys to the lab. If you need [snorts] keys to the lab. If you need [snorts] keys to play around by yourself on your system, play around by yourself on your system, play around by yourself on your system, you can go to code keys page, and you'll you can go to code keys page, and you'll you can go to code keys page, and you'll have access to the keys to access models have access to the keys to access models have access to the keys to access models from top providers like Anthropic, from top providers like Anthropic, from top providers like Anthropic, OpenAI, Google, Deep Seek, and you get a OpenAI, Google, Deep Seek, and you get a OpenAI, Google, Deep Seek, and you get a certain number of tokens each month. Uh certain number of tokens each month. Uh certain number of tokens each month. Uh if you're a paid subscriber to Code if you're a paid subscriber to Code if you're a paid subscriber to Code Cloud, these uh limits are much higher. Cloud, these uh limits are much higher. Cloud, these uh limits are much higher. There are also some sample projects There are also some sample projects There are also some sample projects there to get started with if you need there to get started with if you need there to get started with if you need some ideas. some ideas. some ideas. So, go ahead and complete the lab, and So, go ahead and complete the lab, and So, go ahead and complete the lab, and I'll see you on the other side. You've seen the code. You know what You've seen the code. You know what You've seen the code. You know what every line does. Now, it's time to run every line does. Now, it's time to run every line does. Now, it's time to run it yourself. In this lab, you'll take it yourself. In this lab, you'll take it yourself. In this lab, you'll take the exact program from the last lesson, the exact program from the last lesson, the exact program from the last lesson, the seven lines that call an LLM, and the seven lines that call an LLM, and the seven lines that call an LLM, and run it with a real API key in a real run it with a real API key in a real run it with a real API key in a real terminal. No simulations. Your code, terminal. No simulations. Your code, terminal. No simulations. Your code, your credentials, a live model your credentials, a live model your credentials, a live model responding. Here's how it works. responding. Here's how it works. responding. Here's how it works. The environment is already set up. The environment is already set up. The environment is already set up. Python 3.11 is pre-installed. The Python 3.11 is pre-installed. The Python 3.11 is pre-installed. The virtual environment is active. You just virtual environment is active. You just virtual environment is active. You just need the SDK. Run pip install open ai need the SDK. Run pip install open ai need the SDK. Run pip install open ai and you're connected. Next, you'll and you're connected. Next, you'll and you're connected. Next, you'll create a file called hello_lm.pi. Same code from the lesson. Import open Same code from the lesson. Import open Same code from the lesson. Import open ai, create a client with your API key, ai, create a client with your API key, ai, create a client with your API key, set the model to GPT-4o, send a user set the model to GPT-4o, send a user set the model to GPT-4o, send a user message, and print the response. message, and print the response. message, and print the response. You'll paste it in, save the file, and You'll paste it in, save the file, and You'll paste it in, save the file, and that's your script. Now, run it. that's your script. Now, run it. that's your script. Now, run it. Python 3 hello_llm.py. And just like that, an LLM responds to And just like that, an LLM responds to And just like that, an LLM responds to your prompt in the terminal. Not a your prompt in the terminal. Not a your prompt in the terminal. Not a chatbot interface. Not someone else's chatbot interface. Not someone else's chatbot interface. Not someone else's app. app. app. Your code talking directly to the model. Your code talking directly to the model. Your code talking directly to the model. Here's where it gets interesting. Change Here's where it gets interesting. Change Here's where it gets interesting. Change the prompt to write a one-sentence story the prompt to write a one-sentence story the prompt to write a one-sentence story about a robot. Then, set temperature to about a robot. Then, set temperature to about a robot. Then, set temperature to zero and run it twice. Same story both zero and run it twice. Same story both zero and run it twice. Same story both times. A lonely robot discovered a times. A lonely robot discovered a times. A lonely robot discovered a forgotten garden. forgotten garden. forgotten garden. Word for word, identical. That's Word for word, identical. That's Word for word, identical. That's temperature zero. Deterministic, no temperature zero. Deterministic, no temperature zero. Deterministic, no randomness. Now, change temperature to randomness. Now, change temperature to randomness. Now, change temperature to one and run it again. Different story. one and run it again. Different story. one and run it again. Different story. Run it one more time. Different again. Run it one more time. Different again. Run it one more time. Different again. Same prompt, completely different Same prompt, completely different Same prompt, completely different output. output. output. This is lesson five, running live in This is lesson five, running live in This is lesson five, running live in your terminal. your terminal. your terminal. Temperature isn't theory anymore. You Temperature isn't theory anymore. You Temperature isn't theory anymore. You can see it, control it, and predict what can see it, control it, and predict what can see it, control it, and predict what it does. it does. it does. That's it. You've written and run your That's it. You've written and run your That's it. You've written and run your first LLM program. Every concept from first LLM program. Every concept from first LLM program. Every concept from the last seven lessons, tokens, the last seven lessons, tokens, the last seven lessons, tokens, temperature, prompts, context windows, temperature, prompts, context windows, temperature, prompts, context windows, now has a home in code you wrote. That's now has a home in code you wrote. That's now has a home in code you wrote. That's the foundation everything else in this the foundation everything else in this the foundation everything else in this course builds on. You've just made your first API call. You've just made your first API call. You've just made your first API call. You've seen the model generate text, You've seen the model generate text, You've seen the model generate text, follow instructions, and respond to follow instructions, and respond to follow instructions, and respond to different temperatures. It's impressive, different temperatures. It's impressive, different temperatures. It's impressive, and it's tempting to use it for and it's tempting to use it for and it's tempting to use it for everything. everything. everything. Don't. LLMs are powerful, but they're Don't. LLMs are powerful, but they're Don't. LLMs are powerful, but they're not the right tool for every job. Using not the right tool for every job. Using not the right tool for every job. Using one where regular code would work is one where regular code would work is one where regular code would work is like hiring a translator to read a stop like hiring a translator to read a stop like hiring a translator to read a stop sign. It'll work, but it's slow, sign. It'll work, but it's slow, sign. It'll work, but it's slow, expensive, and completely unnecessary. expensive, and completely unnecessary. expensive, and completely unnecessary. The key to building good AI The key to building good AI The key to building good AI applications, applications, applications, and eventually good AI agents, is and eventually good AI agents, is and eventually good AI agents, is knowing when to call the model and when knowing when to call the model and when knowing when to call the model and when to just write code. to just write code. to just write code. Every task you'll encounter falls into Every task you'll encounter falls into Every task you'll encounter falls into one of two categories. one of two categories. one of two categories. Deterministic tasks, where the same Deterministic tasks, where the same Deterministic tasks, where the same input always produces the same output. input always produces the same output. input always produces the same output. There's one correct answer, and you can There's one correct answer, and you can There's one correct answer, and you can write exact rules to get it. Calculate a write exact rules to get it. Calculate a write exact rules to get it. Calculate a tip, sort a list, convert a date format, tip, sort a list, convert a date format, tip, sort a list, convert a date format, check if an email has an at symbol. A check if an email has an at symbol. A check if an email has an at symbol. A few lines of code handle these few lines of code handle these few lines of code handle these perfectly, instantly, for free, and perfectly, instantly, for free, and perfectly, instantly, for free, and correctly every time. Then, there are correctly every time. Then, there are correctly every time. Then, there are non-deterministic tasks, non-deterministic tasks, non-deterministic tasks, where the right answer depends on where the right answer depends on where the right answer depends on judgment, context, or interpretation. judgment, context, or interpretation. judgment, context, or interpretation. Summarize a legal document. Decide if a Summarize a legal document. Decide if a Summarize a legal document. Decide if a customer email sounds angry or confused. customer email sounds angry or confused. customer email sounds angry or confused. Write a professional reply to a Write a professional reply to a Write a professional reply to a complaint. These require understanding complaint. These require understanding complaint. These require understanding nuance, tone, and meaning. That's nuance, tone, and meaning. That's nuance, tone, and meaning. That's exactly what LLMs are built for. Here's exactly what LLMs are built for. Here's exactly what LLMs are built for. Here's the rule. If the answer is always the the rule. If the answer is always the the rule. If the answer is always the same for the same input, use code. If same for the same input, use code. If same for the same input, use code. If the answer requires judgment or language the answer requires judgment or language the answer requires judgment or language understanding, use an LLM. understanding, use an LLM. understanding, use an LLM. Let's walk through some real examples Let's walk through some real examples Let's walk through some real examples side by side. Calculate shipping cost side by side. Calculate shipping cost side by side. Calculate shipping cost based on weight. Code. It's a formula. based on weight. Code. It's a formula. based on weight. Code. It's a formula. Summarize a product review. Summarize a product review. Summarize a product review. LLM. Requires reading comprehension. LLM. Requires reading comprehension. LLM. Requires reading comprehension. Check if a password meets length Check if a password meets length Check if a password meets length requirements. Code. Len password greater requirements. Code. Len password greater requirements. Code. Len password greater than or equal to eight. Done. Decide if than or equal to eight. Done. Decide if than or equal to eight. Done. Decide if a review is positive or negative. LLM. a review is positive or negative. LLM. a review is positive or negative. LLM. Not bad for the price. Needs judgment. Not bad for the price. Needs judgment. Not bad for the price. Needs judgment. Convert Celsius to Fahrenheit. Convert Celsius to Fahrenheit. Convert Celsius to Fahrenheit. Code. Exact formula. Extract a name and Code. Exact formula. Extract a name and Code. Exact formula. Extract a name and job title from an email. job title from an email. job title from an email. LLM. LLM. LLM. Names appear in unpredictable formats. Names appear in unpredictable formats. Names appear in unpredictable formats. Using an LLM when you don't need one has Using an LLM when you don't need one has Using an LLM when you don't need one has real costs. real costs. real costs. It's slower. An API call takes hundreds It's slower. An API call takes hundreds It's slower. An API call takes hundreds of milliseconds. A line of Python runs of milliseconds. A line of Python runs of milliseconds. A line of Python runs in microseconds. That's a thousandfold in microseconds. That's a thousandfold in microseconds. That's a thousandfold difference. It costs money. Every call difference. It costs money. Every call difference. It costs money. Every call uses tokens, and tokens aren't free. It uses tokens, and tokens aren't free. It uses tokens, and tokens aren't free. It can be wrong. Ask an LLM to compute 12 can be wrong. Ask an LLM to compute 12 can be wrong. Ask an LLM to compute 12 47 * 83, and it might get it wrong. It's 47 * 83, and it might get it wrong. It's 47 * 83, and it might get it wrong. It's predicting tokens, not doing math. And predicting tokens, not doing math. And predicting tokens, not doing math. And it's unpredictable. Even at temperature it's unpredictable. Even at temperature it's unpredictable. Even at temperature zero, output can vary. Here's a real zero, output can vary. Here's a real zero, output can vary. Here's a real example. Say you're building a receipt example. Say you're building a receipt example. Say you're building a receipt generator. The math, subtotal, tax, generator. The math, subtotal, tax, generator. The math, subtotal, tax, total, that's regular code. total, that's regular code. total, that's regular code. Deterministic. But the thank you note at Deterministic. But the thank you note at Deterministic. But the thank you note at the bottom, the bottom, the bottom, "Thanks for your purchase." In a "Thanks for your purchase." In a "Thanks for your purchase." In a different creative way each time, that's different creative way each time, that's different creative way each time, that's the LLM. Each tool does what it's best the LLM. Each tool does what it's best the LLM. Each tool does what it's best at. This idea comes back in a big way at. This idea comes back in a big way at. This idea comes back in a big way later in the course. When we build AI later in the course. When we build AI later in the course. When we build AI agents, they'll have tools, functions agents, they'll have tools, functions agents, they'll have tools, functions they can call to search a database, send they can call to search a database, send they can call to search a database, send an email, or run a calculation. Those an email, or run a calculation. Those an email, or run a calculation. Those tools are regular code. The LLM's job is tools are regular code. The LLM's job is tools are regular code. The LLM's job is to decide which tool to use and when, to decide which tool to use and when, to decide which tool to use and when, the judgment call. the judgment call. the judgment call. The tool itself handles the The tool itself handles the The tool itself handles the deterministic work. That's the pattern deterministic work. That's the pattern deterministic work. That's the pattern behind every good AI system. Let the LLM behind every good AI system. Let the LLM behind every good AI system. Let the LLM think. Let code execute. In the last lesson, you sent a single In the last lesson, you sent a single In the last lesson, you sent a single message to the API. One user, one message to the API. One user, one message to the API. One user, one question. That works. question. That works. question. That works. But in practice, the messages list is But in practice, the messages list is But in practice, the messages list is designed to carry a full conversation. designed to carry a full conversation. designed to carry a full conversation. And a conversation has more than one And a conversation has more than one And a conversation has more than one voice. voice. voice. The API supports three distinct roles, The API supports three distinct roles, The API supports three distinct roles, user, system, and assistant. Each user, system, and assistant. Each user, system, and assistant. Each message is labeled with one of these message is labeled with one of these message is labeled with one of these roles, and the model reads the full list roles, and the model reads the full list roles, and the model reads the full list in order before generating its reply. in order before generating its reply. in order before generating its reply. The user role is what you've already The user role is what you've already The user role is what you've already seen. It's the human side of the seen. It's the human side of the seen. It's the human side of the conversation. conversation. conversation. You send a question, the model answers. You send a question, the model answers. You send a question, the model answers. The system role is the developer's The system role is the developer's The system role is the developer's message to the model. The user never message to the model. The user never message to the model. The user never sees it. It's instructions set before sees it. It's instructions set before sees it. It's instructions set before the conversation begins. Think of it as the conversation begins. Think of it as the conversation begins. Think of it as the briefing you give a new employee the briefing you give a new employee the briefing you give a new employee before their first customer call. It before their first customer call. It before their first customer call. It sets the context for everything that sets the context for everything that sets the context for everything that follows. Here's a concrete example. follows. Here's a concrete example. follows. Here's a concrete example. Without a system message, you ask the Without a system message, you ask the Without a system message, you ask the model how to fix a bug, model how to fix a bug, model how to fix a bug, and you get a helpful, general, fairly and you get a helpful, general, fairly and you get a helpful, general, fairly verbose answer. Add a system message verbose answer. Add a system message verbose answer. Add a system message that says, "You are a senior engineer that says, "You are a senior engineer that says, "You are a senior engineer doing a code review. Be direct and doing a code review. Be direct and doing a code review. Be direct and critical." critical." critical." And the same question gets a completely And the same question gets a completely And the same question gets a completely different response. The system message different response. The system message different response. The system message shifted which patterns the model draws shifted which patterns the model draws shifted which patterns the model draws on. What can you put in a system on. What can you put in a system on. What can you put in a system message? A persona. message? A persona. message? A persona. "You are a friendly customer support "You are a friendly customer support "You are a friendly customer support agent." Constraints. "Never discuss agent." Constraints. "Never discuss agent." Constraints. "Never discuss competitors." competitors." competitors." Format instructions. "Always respond in Format instructions. "Always respond in Format instructions. "Always respond in bullet points." bullet points." bullet points." Background knowledge. Background knowledge. Background knowledge. "The user is a paid subscriber." Every "The user is a paid subscriber." Every "The user is a paid subscriber." Every AI tool you've used has a system message AI tool you've used has a system message AI tool you've used has a system message running behind the scenes. running behind the scenes. running behind the scenes. ChatGPT, travel booking assistants, ChatGPT, travel booking assistants, ChatGPT, travel booking assistants, banking chatbots. banking chatbots. banking chatbots. They're not fundamentally different They're not fundamentally different They're not fundamentally different models. They're the same technology models. They're the same technology models. They're the same technology steered by different system messages. steered by different system messages. steered by different system messages. Now, the assistant role, Now, the assistant role, Now, the assistant role, this is the model's own previous replies this is the model's own previous replies this is the model's own previous replies added back into the conversation added back into the conversation added back into the conversation history. history. history. Why would you include the model's own Why would you include the model's own Why would you include the model's own messages? Because the model has no messages? Because the model has no messages? Because the model has no memory between calls. memory between calls. memory between calls. Every API call is stateless, and it Every API call is stateless, and it Every API call is stateless, and it starts completely fresh. The only memory starts completely fresh. The only memory starts completely fresh. The only memory it has is what you put in the messages it has is what you put in the messages it has is what you put in the messages list. So, if you want a multi-turn list. So, if you want a multi-turn list. So, if you want a multi-turn conversation, you feed the history back conversation, you feed the history back conversation, you feed the history back in manually. in manually. in manually. You send the system message, the user's You send the system message, the user's You send the system message, the user's first question, the assistant's previous first question, the assistant's previous first question, the assistant's previous reply, and the user's follow-up. reply, and the user's follow-up. reply, and the user's follow-up. The model sees the full exchange. The model sees the full exchange. The model sees the full exchange. When it reads, "What's the population of When it reads, "What's the population of When it reads, "What's the population of that city?" it knows that city means that city?" it knows that city means that city?" it knows that city means Paris, Paris, Paris, because the previous turn is right there because the previous turn is right there because the previous turn is right there in the list. Without the assistant in the list. Without the assistant in the list. Without the assistant message in the history, the model would message in the history, the model would message in the history, the model would have no idea what that city refers to. have no idea what that city refers to. have no idea what that city refers to. This is how every chatbot maintains This is how every chatbot maintains This is how every chatbot maintains context. Not with real memory, but by context. Not with real memory, but by context. Not with real memory, but by replaying the conversation on every replaying the conversation on every replaying the conversation on every single call. single call. single call. Three roles. System sets the behavior. Three roles. System sets the behavior. Three roles. System sets the behavior. User provides the input. Assistant User provides the input. Assistant User provides the input. Assistant maintains the history. Together, they maintains the history. Together, they maintains the history. Together, they give you full control over how the model give you full control over how the model give you full control over how the model responds and what it remembers. responds and what it remembers. responds and what it remembers. Welcome to this section on architecture. Welcome to this section on architecture. Welcome to this section on architecture. Now that you understand how LLMs work Now that you understand how LLMs work Now that you understand how LLMs work under the hood, it's time to ask the under the hood, it's time to ask the under the hood, it's time to ask the next question. How do we make them next question. How do we make them next question. How do we make them actually do things? actually do things? actually do things? On their own, LLMs can only generate On their own, LLMs can only generate On their own, LLMs can only generate text. text. text. But when you give them tools, the But when you give them tools, the But when you give them tools, the ability to search the web, query a ability to search the web, query a ability to search the web, query a database, or call an API, they become database, or call an API, they become database, or call an API, they become much more powerful. much more powerful. much more powerful. In this section, you'll learn how tool In this section, you'll learn how tool In this section, you'll learn how tool calling works, and build your first tool calling works, and build your first tool calling works, and build your first tool calling application. calling application. calling application. Then, we'll zoom out and look at the big Then, we'll zoom out and look at the big Then, we'll zoom out and look at the big picture. Not every AI system needs to be picture. Not every AI system needs to be picture. Not every AI system needs to be fully autonomous agent. Sometimes, a fully autonomous agent. Sometimes, a fully autonomous agent. Sometimes, a simple, predictable workflow is the simple, predictable workflow is the simple, predictable workflow is the better choice. We'll explore the better choice. We'll explore the better choice. We'll explore the difference between AI workflows and AI difference between AI workflows and AI difference between AI workflows and AI agents, and just as importantly, when to agents, and just as importantly, when to agents, and just as importantly, when to use each one. use each one. use each one. By the end of this section, you'll have By the end of this section, you'll have By the end of this section, you'll have a clear mental model for how AI systems a clear mental model for how AI systems a clear mental model for how AI systems are structured, and you'll be ready to are structured, and you'll be ready to are structured, and you'll be ready to start building agents of your own. LLMs can only generate text. They can't LLMs can only generate text. They can't LLMs can only generate text. They can't check your calendar, send an email, or check your calendar, send an email, or check your calendar, send an email, or search the web. On their own, they're search the web. On their own, they're search the web. On their own, they're stuck inside their training data. Tools stuck inside their training data. Tools stuck inside their training data. Tools give LLMs the ability to do things. A give LLMs the ability to do things. A give LLMs the ability to do things. A tool is a function that the LLM can call tool is a function that the LLM can call tool is a function that the LLM can call to interact with the outside world. to interact with the outside world. to interact with the outside world. Without tools, an LLM can only answer Without tools, an LLM can only answer Without tools, an LLM can only answer from what it learned during training. from what it learned during training. from what it learned during training. Ask it, "What's on my calendar today?" Ask it, "What's on my calendar today?" Ask it, "What's on my calendar today?" and it has no idea. It can't access your and it has no idea. It can't access your and it has no idea. It can't access your calendar. But when you give it a tool, calendar. But when you give it a tool, calendar. But when you give it a tool, everything changes. Let's walk through everything changes. Let's walk through everything changes. Let's walk through what happens step by step. You ask, what happens step by step. You ask, what happens step by step. You ask, "What's on my calendar today?" "What's on my calendar today?" "What's on my calendar today?" The LLM reads that and realizes it needs The LLM reads that and realizes it needs The LLM reads that and realizes it needs real-time data. So, instead of guessing, real-time data. So, instead of guessing, real-time data. So, instead of guessing, it decides to call a tool. It calls it decides to call a tool. It calls it decides to call a tool. It calls check calendar with today's date. The check calendar with today's date. The check calendar with today's date. The tool returns your events. tool returns your events. tool returns your events. The LLM responds, "You have two things The LLM responds, "You have two things The LLM responds, "You have two things today, team standup at 10:00 a.m. and a today, team standup at 10:00 a.m. and a today, team standup at 10:00 a.m. and a dentist appointment at 2:00 p.m." dentist appointment at 2:00 p.m." dentist appointment at 2:00 p.m." The LLM didn't know your schedule. It The LLM didn't know your schedule. It The LLM didn't know your schedule. It couldn't have. But it recognized it had couldn't have. But it recognized it had couldn't have. But it recognized it had a calendar tool available, called it, a calendar tool available, called it, a calendar tool available, called it, got back the raw data, and turned that got back the raw data, and turned that got back the raw data, and turned that into a useful response. The tool gave it into a useful response. The tool gave it into a useful response. The tool gave it the facts, and the LLM made them useful. the facts, and the LLM made them useful. the facts, and the LLM made them useful. A tool has three parts. Name, what the A tool has three parts. Name, what the A tool has three parts. Name, what the tool is called. Description, tool is called. Description, tool is called. Description, what the tool does, which the LLM reads what the tool does, which the LLM reads what the tool does, which the LLM reads to decide when to use it. And to decide when to use it. And to decide when to use it. And parameters, uh what inputs the tool parameters, uh what inputs the tool parameters, uh what inputs the tool needs. needs. needs. The LLM reads your tool definitions and The LLM reads your tool definitions and The LLM reads your tool definitions and decides whether to call a tool and which decides whether to call a tool and which decides whether to call a tool and which one. This decision is part of the one. This decision is part of the one. This decision is part of the model's text generation, not a separate model's text generation, not a separate model's text generation, not a separate system. Here are the most common tool system. Here are the most common tool system. Here are the most common tool types. Search tools retrieve types. Search tools retrieve types. Search tools retrieve information, web search, database information, web search, database information, web search, database queries. Calculation tools perform queries. Calculation tools perform queries. Calculation tools perform precise math, calculators, unit precise math, calculators, unit precise math, calculators, unit converters. API call tools interact with converters. API call tools interact with converters. API call tools interact with services, send email, create calendar services, send email, create calendar services, send email, create calendar events. File operation tools read or events. File operation tools read or events. File operation tools read or write files. Code execution tools run write files. Code execution tools run write files. Code execution tools run Python or shell commands. A personal Python or shell commands. A personal Python or shell commands. A personal assistant agent might use all of these, assistant agent might use all of these, assistant agent might use all of these, search the web for restaurants, check search the web for restaurants, check search the web for restaurants, check your calendar for free slots, send an your calendar for free slots, send an your calendar for free slots, send an email to confirm a reservation, and save email to confirm a reservation, and save email to confirm a reservation, and save the confirmation as a file. Tools are the confirmation as a file. Tools are the confirmation as a file. Tools are just functions, nothing magical. They're just functions, nothing magical. They're just functions, nothing magical. They're regular functions that you write and regular functions that you write and regular functions that you write and expose to the LLM. The LLM decides when expose to the LLM. The LLM decides when expose to the LLM. The LLM decides when to call them, but your code actually to call them, but your code actually to call them, but your code actually executes them. You define the tools, the executes them. You define the tools, the executes them. You define the tools, the LLM decides which to call, your code LLM decides which to call, your code LLM decides which to call, your code runs the function, and the result goes runs the function, and the result goes runs the function, and the result goes back to the LLM. The LLM never runs the back to the LLM. The LLM never runs the back to the LLM. The LLM never runs the tools itself. It outputs a structured tools itself. It outputs a structured tools itself. It outputs a structured request, and your application handles request, and your application handles request, and your application handles execution. This is an important safety execution. This is an important safety execution. This is an important safety boundary. You control what the tools can boundary. You control what the tools can boundary. You control what the tools can actually do. Lesson nine, tool integration. In the Lesson nine, tool integration. In the Lesson nine, tool integration. In the last lesson, we learned what tools are. last lesson, we learned what tools are. last lesson, we learned what tools are. Now let's look at how they work in Now let's look at how they work in Now let's look at how they work in practice through the tool use loop. When practice through the tool use loop. When practice through the tool use loop. When you give an LLM tools, it can respond you give an LLM tools, it can respond you give an LLM tools, it can respond with text, or it can output a specific with text, or it can output a specific with text, or it can output a specific tool call. If it asks for a tool, your tool call. If it asks for a tool, your tool call. If it asks for a tool, your code must execute the action and send code must execute the action and send code must execute the action and send the result back. This creates a loop. the result back. This creates a loop. the result back. This creates a loop. This pattern is often called function This pattern is often called function This pattern is often called function calling. Let's look at a scheduling calling. Let's look at a scheduling calling. Let's look at a scheduling example. If a user asks to schedule example. If a user asks to schedule example. If a user asks to schedule lunch, the LLM might first check the lunch, the LLM might first check the lunch, the LLM might first check the calendar, then search for a contact, calendar, then search for a contact, calendar, then search for a contact, finally create the event. The model can finally create the event. The model can finally create the event. The model can even handle multiple tool calls. If your even handle multiple tool calls. If your even handle multiple tool calls. If your request involves several pieces of request involves several pieces of request involves several pieces of information, the model makes those calls information, the model makes those calls information, the model makes those calls in sequence, or sometimes in parallel, in sequence, or sometimes in parallel, in sequence, or sometimes in parallel, before giving you a final answer. In before giving you a final answer. In before giving you a final answer. In code, it looks like this. You have a code, it looks like this. You have a code, it looks like this. You have a message list. If the LLM generates tool message list. If the LLM generates tool message list. If the LLM generates tool calls, your code iterates through them, calls, your code iterates through them, calls, your code iterates through them, executes each, and appends the result executes each, and appends the result executes each, and appends the result back to the messages for the LLM to back to the messages for the LLM to back to the messages for the LLM to process. process. process. This whole pattern is often called This whole pattern is often called This whole pattern is often called function calling. Remember, the LLM function calling. Remember, the LLM function calling. Remember, the LLM isn't running the code, it just outputs isn't running the code, it just outputs isn't running the code, it just outputs the intent in JSON. Your application is the intent in JSON. Your application is the intent in JSON. Your application is the middleman that actually does the the middleman that actually does the the middleman that actually does the work. Tool integration is what turns a work. Tool integration is what turns a work. Tool integration is what turns a simple text generator into a powerful simple text generator into a powerful simple text generator into a powerful agent capable of doing actual work. This agent capable of doing actual work. This agent capable of doing actual work. This is the foundation of everything we'll is the foundation of everything we'll is the foundation of everything we'll build next. Time to put this into practice. In this Time to put this into practice. In this Time to put this into practice. In this lab, you'll build a Python application lab, you'll build a Python application lab, you'll build a Python application where the language model decides when to where the language model decides when to where the language model decides when to call tools on your behalf. You'll define call tools on your behalf. You'll define call tools on your behalf. You'll define tool schemas, write handler functions, tool schemas, write handler functions, tool schemas, write handler functions, and wire up a tool call loop, the same and wire up a tool call loop, the same and wire up a tool call loop, the same pattern that powers every AI agent. pattern that powers every AI agent. pattern that powers every AI agent. Here's how the lab works. The Here's how the lab works. The Here's how the lab works. The environment is ready. Python 3.11, a environment is ready. Python 3.11, a environment is ready. Python 3.11, a virtual environment, and the OpenAI SDK virtual environment, and the OpenAI SDK virtual environment, and the OpenAI SDK are all pre-installed. Your working are all pre-installed. Your working are all pre-installed. Your working directory is {slash} root {slash} code. directory is {slash} root {slash} code. directory is {slash} root {slash} code. First, you'll create a file called First, you'll create a file called First, you'll create a file called tool_app.py. tool_app.py. tool_app.py. It starts with the basics, import OS and It starts with the basics, import OS and It starts with the basics, import OS and OpenAI, create a client using your API OpenAI, create a client using your API OpenAI, create a client using your API key and base URL from environment key and base URL from environment key and base URL from environment variables, set a system message, and variables, set a system message, and variables, set a system message, and initialize an empty messages list. Next, initialize an empty messages list. Next, initialize an empty messages list. Next, you'll define your first tool, and you'll define your first tool, and you'll define your first tool, and check_calendar. The model reads the check_calendar. The model reads the check_calendar. The model reads the tool's name, description, and parameter tool's name, description, and parameter tool's name, description, and parameter schema to decide when to call it. You schema to decide when to call it. You schema to decide when to call it. You wrap it in a tools list following the wrap it in a tools list following the wrap it in a tools list following the JSON schema format, type function, a JSON schema format, type function, a JSON schema format, type function, a name, a description, and a parameters name, a description, and a parameters name, a description, and a parameters object with a required date string. Then object with a required date string. Then object with a required date string. Then you write the actual Python function, a you write the actual Python function, a you write the actual Python function, a mock that returns hardcoded calendar mock that returns hardcoded calendar mock that returns hardcoded calendar events, and a dispatcher called events, and a dispatcher called events, and a dispatcher called execute_tool execute_tool execute_tool that maps tool names to their Python that maps tool names to their Python that maps tool names to their Python functions. Now the interesting part. You functions. Now the interesting part. You functions. Now the interesting part. You add the user message, "What's on my add the user message, "What's on my add the user message, "What's on my calendar today?" and call the API with calendar today?" and call the API with calendar today?" and call the API with your tools list. The model doesn't your tools list. The model doesn't your tools list. The model doesn't answer directly. Instead, it returns answer directly. Instead, it returns answer directly. Instead, it returns finish_reason tool_calls, telling your finish_reason tool_calls, telling your finish_reason tool_calls, telling your code, "Execute this function first." You code, "Execute this function first." You code, "Execute this function first." You extract the tool name and arguments, run extract the tool name and arguments, run extract the tool name and arguments, run your local function, and append the your local function, and append the your local function, and append the result as a tool message. Then you call result as a tool message. Then you call result as a tool message. Then you call the API again. This time the model has the API again. This time the model has the API again. This time the model has the tool output and composes a final the tool output and composes a final the tool output and composes a final answer. answer. answer. Run it. Run it. Run it. Python 3 Python 3 Python 3 tool_app.py. tool_app.py. tool_app.py. The output mentions your calendar The output mentions your calendar The output mentions your calendar events, a team standup at 10:00 a.m. and events, a team standup at 10:00 a.m. and events, a team standup at 10:00 a.m. and a dentist appointment at 2:00 p.m. a dentist appointment at 2:00 p.m. a dentist appointment at 2:00 p.m. The model chose the right tool, your The model chose the right tool, your The model chose the right tool, your code executed it, and the model turned code executed it, and the model turned code executed it, and the model turned raw data into a natural language answer. raw data into a natural language answer. raw data into a natural language answer. That's the complete tool call loop, and That's the complete tool call loop, and That's the complete tool call loop, and it's the foundation every AI agent is it's the foundation every AI agent is it's the foundation every AI agent is built on. Now that you know how LLMs use tools, Now that you know how LLMs use tools, Now that you know how LLMs use tools, let's look at how developers combine LLM let's look at how developers combine LLM let's look at how developers combine LLM calls and tools into larger systems. calls and tools into larger systems. calls and tools into larger systems. These are called AI workflows, systems These are called AI workflows, systems These are called AI workflows, systems where LLMs and tools are orchestrated where LLMs and tools are orchestrated where LLMs and tools are orchestrated through predefined code paths. through predefined code paths. through predefined code paths. The keyword is predefined. In a The keyword is predefined. In a The keyword is predefined. In a workflow, the developer decides the workflow, the developer decides the workflow, the developer decides the steps in advance. The LLM does the heavy steps in advance. The LLM does the heavy steps in advance. The LLM does the heavy lifting at each step, but the flow is lifting at each step, but the flow is lifting at each step, but the flow is controlled by your code. The simplest controlled by your code. The simplest controlled by your code. The simplest pattern, you break a task into pattern, you break a task into pattern, you break a task into sequential steps where each LLM call sequential steps where each LLM call sequential steps where each LLM call processes the output of the previous processes the output of the previous processes the output of the previous one. one. one. Say you want to draft an email and then Say you want to draft an email and then Say you want to draft an email and then review it. Step one, the LLM writes the review it. Step one, the LLM writes the review it. Step one, the LLM writes the email. Step two, a second LLM call email. Step two, a second LLM call email. Step two, a second LLM call reviews what the first one wrote and reviews what the first one wrote and reviews what the first one wrote and polishes it. You can add programmatic polishes it. You can add programmatic polishes it. You can add programmatic checks between steps, validate the checks between steps, validate the checks between steps, validate the output before passing it to the next output before passing it to the next output before passing it to the next call. In the routing pattern, you call. In the routing pattern, you call. In the routing pattern, you classify the input first, then send it classify the input first, then send it classify the input first, then send it to the right handler. to the right handler. to the right handler. &gt;&gt; [snorts] &gt;&gt; [snorts] &gt;&gt; [snorts] &gt;&gt; A classifier LLM reads the user's &gt;&gt; A classifier LLM reads the user's &gt;&gt; A classifier LLM reads the user's message and decides which route to take. message and decides which route to take. message and decides which route to take. Is this a simple question, a scheduling Is this a simple question, a scheduling Is this a simple question, a scheduling request, or a complex travel planning request, or a complex travel planning request, or a complex travel planning task? This lets you use cheaper models task? This lets you use cheaper models task? This lets you use cheaper models for easy tasks, and reserve expensive for easy tasks, and reserve expensive for easy tasks, and reserve expensive models for hard ones. Run independent models for hard ones. Run independent models for hard ones. Run independent subtasks at the same time instead of one subtasks at the same time instead of one subtasks at the same time instead of one after another. Sectioning splits the after another. Sectioning splits the after another. Sectioning splits the task into different parts that run task into different parts that run task into different parts that run simultaneously. For example, "Plan my simultaneously. For example, "Plan my simultaneously. For example, "Plan my Thursday" kicks off checking the Thursday" kicks off checking the Thursday" kicks off checking the calendar, checking emails, and checking calendar, checking emails, and checking calendar, checking emails, and checking the weather, then combines them into a the weather, then combines them into a the weather, then combines them into a daily briefing. The other flavor is daily briefing. The other flavor is daily briefing. The other flavor is voting. Run the same task multiple times voting. Run the same task multiple times voting. Run the same task multiple times and take the consensus. Ask three and take the consensus. Ask three and take the consensus. Ask three models, "Is this content safe?" and go models, "Is this content safe?" and go models, "Is this content safe?" and go with the majority In this pattern, a with the majority In this pattern, a with the majority In this pattern, a central LLM dynamically decides what central LLM dynamically decides what central LLM dynamically decides what subtasks are needed and delegates them subtasks are needed and delegates them subtasks are needed and delegates them to worker LLMs. to worker LLMs. to worker LLMs. The orchestrator reads the input, breaks The orchestrator reads the input, breaks The orchestrator reads the input, breaks it into pieces, farms them out, and then it into pieces, farms them out, and then it into pieces, farms them out, and then combines the results. Unlike combines the results. Unlike combines the results. Unlike parallelization, the subtasks aren't parallelization, the subtasks aren't parallelization, the subtasks aren't predefined. The orchestrator figures out predefined. The orchestrator figures out predefined. The orchestrator figures out what work is needed based on the input. what work is needed based on the input. what work is needed based on the input. This pattern uses two LLMs working This pattern uses two LLMs working This pattern uses two LLMs working together. One generates a response, the together. One generates a response, the together. One generates a response, the other evaluates it and gives feedback. other evaluates it and gives feedback. other evaluates it and gives feedback. The generator takes that feedback, The generator takes that feedback, The generator takes that feedback, improves its output, and the cycle improves its output, and the cycle improves its output, and the cycle repeats until the quality is good repeats until the quality is good repeats until the quality is good enough. enough. enough. This is useful when quality matters more This is useful when quality matters more This is useful when quality matters more than speed, like translating nuanced than speed, like translating nuanced than speed, like translating nuanced text or writing production code. In text or writing production code. In text or writing production code. In every pattern above, the code controls every pattern above, the code controls every pattern above, the code controls the flow. The developer decides which the flow. The developer decides which the flow. The developer decides which steps happen, in what order, what gets steps happen, in what order, what gets steps happen, in what order, what gets passed between them, and when to stop. passed between them, and when to stop. passed between them, and when to stop. The LLM is powerful, but it's operating The LLM is powerful, but it's operating The LLM is powerful, but it's operating within a structure you designed. This is within a structure you designed. This is within a structure you designed. This is what makes workflows predictable and what makes workflows predictable and what makes workflows predictable and debuggable. Lesson 11, Lesson 11, Lesson 11, workflows versus agents. workflows versus agents. workflows versus agents. In the last lesson, you saw how In the last lesson, you saw how In the last lesson, you saw how developers combine LLM calls and tools developers combine LLM calls and tools developers combine LLM calls and tools into workflows. In a workflow, you into workflows. In a workflow, you into workflows. In a workflow, you define the flow, which steps happen, in define the flow, which steps happen, in define the flow, which steps happen, in what order, when to stop. You draw the what order, when to stop. You draw the what order, when to stop. You draw the map before the journey starts. But what map before the journey starts. But what map before the journey starts. But what if you can't draw the map in advance? A if you can't draw the map in advance? A if you can't draw the map in advance? A customer emails saying their account was customer emails saying their account was customer emails saying their account was charged twice. To investigate, you might charged twice. To investigate, you might charged twice. To investigate, you might check payment history, but what you find check payment history, but what you find check payment history, but what you find there determines the next step. Maybe there determines the next step. Maybe there determines the next step. Maybe you need to look at their subscription, you need to look at their subscription, you need to look at their subscription, maybe cross-reference a refund policy, maybe cross-reference a refund policy, maybe cross-reference a refund policy, maybe check for an existing support maybe check for an existing support maybe check for an existing support ticket. You don't know until you're ticket. You don't know until you're ticket. You don't know until you're already inside the problem. Or a user already inside the problem. Or a user already inside the problem. Or a user asks, "Find me a cheap flight to London asks, "Find me a cheap flight to London asks, "Find me a cheap flight to London next weekend." next weekend." next weekend." There isn't one. Do you stop, check the There isn't one. Do you stop, check the There isn't one. Do you stop, check the following weekend, try a nearby airport? following weekend, try a nearby airport? following weekend, try a nearby airport? The right move depends on what you just The right move depends on what you just The right move depends on what you just discovered. Or a developer asks an AI, discovered. Or a developer asks an AI, discovered. Or a developer asks an AI, "Why is this test failing?" Answering "Why is this test failing?" Answering "Why is this test failing?" Answering that might mean reading files, running that might mean reading files, running that might mean reading files, running commands, checking logs. No two bugs commands, checking logs. No two bugs commands, checking logs. No two bugs follow the same path. In all these follow the same path. In all these follow the same path. In all these cases, the steps can't be predefined. cases, the steps can't be predefined. cases, the steps can't be predefined. Someone, or something, needs to decide Someone, or something, needs to decide Someone, or something, needs to decide what to do next on the fly. what to do next on the fly. what to do next on the fly. That's what we're looking at now. That's what we're looking at now. That's what we're looking at now. Both workflows and agents are agentic Both workflows and agents are agentic Both workflows and agents are agentic systems. They combine LLMs with tools to systems. They combine LLMs with tools to systems. They combine LLMs with tools to accomplish tasks. The difference is who accomplish tasks. The difference is who accomplish tasks. The difference is who controls the flow. controls the flow. controls the flow. In a workflow, you, the developer, In a workflow, you, the developer, In a workflow, you, the developer, decide the steps in advance. The LLM decide the steps in advance. The LLM decide the steps in advance. The LLM executes each step, but the sequence, executes each step, but the sequence, executes each step, but the sequence, branching, and logic are defined in your branching, and logic are defined in your branching, and logic are defined in your code. Your code decides, step one, call code. Your code decides, step one, call code. Your code decides, step one, call LLM to classify input. Step two, based LLM to classify input. Step two, based LLM to classify input. Step two, based on classification, call LLM to generate on classification, call LLM to generate on classification, call LLM to generate response. Step three, call LLM to response. Step three, call LLM to response. Step three, call LLM to translate response. The LLM is powerful translate response. The LLM is powerful translate response. The LLM is powerful at each step, but it doesn't choose what at each step, but it doesn't choose what at each step, but it doesn't choose what to do next. Your code does. In an agent, to do next. Your code does. In an agent, to do next. Your code does. In an agent, the LLM decides what to do next. It the LLM decides what to do next. It the LLM decides what to do next. It looks at the current situation, picks a looks at the current situation, picks a looks at the current situation, picks a tool, uses the result to decide the next tool, uses the result to decide the next tool, uses the result to decide the next action, and keeps going until the task action, and keeps going until the task action, and keeps going until the task is done. The LLM decides, "I need to is done. The LLM decides, "I need to is done. The LLM decides, "I need to check the calendar." Calls calendar check the calendar." Calls calendar check the calendar." Calls calendar tool. "There's a conflict. Let me find tool. "There's a conflict. Let me find tool. "There's a conflict. Let me find alternatives." Calls search tool. alternatives." Calls search tool. alternatives." Calls search tool. "I found an option that works." "I found an option that works." "I found an option that works." Generates final response. The developer Generates final response. The developer Generates final response. The developer provides the tools and guardrails. The provides the tools and guardrails. The provides the tools and guardrails. The LLM decides how to use them. Let's put LLM decides how to use them. Let's put LLM decides how to use them. Let's put these side by side to see the these side by side to see the these side by side to see the differences clearly. differences clearly. differences clearly. In a workflow, the developer defines the In a workflow, the developer defines the In a workflow, the developer defines the steps. Execution is predictable, and you steps. Execution is predictable, and you steps. Execution is predictable, and you know exactly how many LLM calls you'll know exactly how many LLM calls you'll know exactly how many LLM calls you'll make. In an agent, the LLM decides the make. In an agent, the LLM decides the make. In an agent, the LLM decides the steps. Execution is dynamic, and the steps. Execution is dynamic, and the steps. Execution is dynamic, and the number of calls varies depending on the number of calls varies depending on the number of calls varies depending on the task. It's not always a clean binary. task. It's not always a clean binary. task. It's not always a clean binary. Most real systems fall somewhere on a Most real systems fall somewhere on a Most real systems fall somewhere on a spectrum. On the far left, you have a spectrum. On the far left, you have a spectrum. On the far left, you have a pure workflow, where every step is pure workflow, where every step is pure workflow, where every step is predefined. Move right, and you start predefined. Move right, and you start predefined. Move right, and you start giving the LLM more control. First, it giving the LLM more control. First, it giving the LLM more control. First, it picks a path, routing. Then, it decides picks a path, routing. Then, it decides picks a path, routing. Then, it decides what subtasks to create, orchestrator. what subtasks to create, orchestrator. what subtasks to create, orchestrator. And at the far right, you have a fully And at the far right, you have a fully And at the far right, you have a fully autonomous agent running in a loop with autonomous agent running in a loop with autonomous agent running in a loop with full control. full control. full control. Moving right on this spectrum means Moving right on this spectrum means Moving right on this spectrum means giving the LLM more control. giving the LLM more control. giving the LLM more control. More control means more flexibility, and More control means more flexibility, and More control means more flexibility, and but also more cost, more latency, and but also more cost, more latency, and but also more cost, more latency, and harder debugging. Ask yourself, "Can I harder debugging. Ask yourself, "Can I harder debugging. Ask yourself, "Can I draw a flowchart of every possible path draw a flowchart of every possible path draw a flowchart of every possible path before running it?" Yes, uh um before running it?" Yes, uh um before running it?" Yes, uh um it's a workflow. No, it's an agent. Lesson 12. Lesson 12. Lesson 12. When to use each. When to use each. When to use each. You now know the difference between You now know the difference between You now know the difference between workflows and agents, but knowing the workflows and agents, but knowing the workflows and agents, but knowing the difference doesn't tell you which one to difference doesn't tell you which one to difference doesn't tell you which one to build. That's a practical question that build. That's a practical question that build. That's a practical question that comes up every time you start a new comes up every time you start a new comes up every time you start a new project. The wrong choice has real project. The wrong choice has real project. The wrong choice has real consequences. It doesn't just make your consequences. It doesn't just make your consequences. It doesn't just make your code harder to write. It makes it more code harder to write. It makes it more code harder to write. It makes it more expensive to run, harder to debug, and expensive to run, harder to debug, and expensive to run, harder to debug, and more likely to break. There's a more likely to break. There's a more likely to break. There's a principle that cuts through the noise. principle that cuts through the noise. principle that cuts through the noise. Find the simplest solution possible, and Find the simplest solution possible, and Find the simplest solution possible, and only increase complexity when needed. only increase complexity when needed. only increase complexity when needed. Most tasks don't need an agent. A Most tasks don't need an agent. A Most tasks don't need an agent. A workflow, or even a single LLM call, is workflow, or even a single LLM call, is workflow, or even a single LLM call, is often enough. Before building anything, often enough. Before building anything, often enough. Before building anything, ask yourself the simplest question ask yourself the simplest question ask yourself the simplest question first. Can a single LLM call handle first. Can a single LLM call handle first. Can a single LLM call handle this? If yes, just do that. If you need this? If yes, just do that. If you need this? If yes, just do that. If you need multiple calls working in sequence, you multiple calls working in sequence, you multiple calls working in sequence, you want a workflow. And if the LLM needs to want a workflow. And if the LLM needs to want a workflow. And if the LLM needs to decide its own steps based on what it decide its own steps based on what it decide its own steps based on what it discovers along the way, that's when you discovers along the way, that's when you discovers along the way, that's when you consider an agent. Each step up the consider an agent. Each step up the consider an agent. Each step up the ladder adds cost, latency, and ladder adds cost, latency, and ladder adds cost, latency, and complexity. Only climb when the simpler complexity. Only climb when the simpler complexity. Only climb when the simpler approach genuinely can't do the job. approach genuinely can't do the job. approach genuinely can't do the job. Workflows shine when the problem is well Workflows shine when the problem is well Workflows shine when the problem is well understood. If you already know the understood. If you already know the understood. If you already know the steps, even if the inputs change, a steps, even if the inputs change, a steps, even if the inputs change, a workflow is almost always the better workflow is almost always the better workflow is almost always the better choice. Think about email classification choice. Think about email classification choice. Think about email classification and response, daily briefing generation, and response, daily briefing generation, and response, daily briefing generation, data extraction, translation chains. data extraction, translation chains. data extraction, translation chains. These tasks have a clear shape. You can These tasks have a clear shape. You can These tasks have a clear shape. You can describe the steps before you run them. describe the steps before you run them. describe the steps before you run them. The steps don't change based on what the The steps don't change based on what the The steps don't change based on what the LLM finds. The flow is predictable. LLM finds. The flow is predictable. LLM finds. The flow is predictable. Every input takes the same path. Agents Every input takes the same path. Agents Every input takes the same path. Agents earn their complexity when the task is earn their complexity when the task is earn their complexity when the task is genuinely open-ended, when the right genuinely open-ended, when the right genuinely open-ended, when the right path depends on what the LLM discovers path depends on what the LLM discovers path depends on what the LLM discovers along the way. Think about trip along the way. Think about trip along the way. Think about trip planning, personal assistant tasks, planning, personal assistant tasks, planning, personal assistant tasks, complex scheduling, coding assistance. complex scheduling, coding assistance. complex scheduling, coding assistance. These tasks don't have a fixed shape. These tasks don't have a fixed shape. These tasks don't have a fixed shape. The steps emerge as the work happens. The steps emerge as the work happens. The steps emerge as the work happens. The LLM needs flexibility. It needs to The LLM needs flexibility. It needs to The LLM needs flexibility. It needs to adapt based on what it finds. Different adapt based on what it finds. Different adapt based on what it finds. Different inputs take different paths. The LLM inputs take different paths. The LLM inputs take different paths. The LLM decides what to do next. Getting it decides what to do next. Getting it decides what to do next. Getting it wrong has a real cost. Using an agent wrong has a real cost. Using an agent wrong has a real cost. Using an agent when a workflow would suffice means when a workflow would suffice means when a workflow would suffice means you're paying more per request. You're you're paying more per request. You're you're paying more per request. You're waiting longer for responses. You're waiting longer for responses. You're waiting longer for responses. You're spending more time debugging paths that spending more time debugging paths that spending more time debugging paths that change every run. You've also given the change every run. You've also given the change every run. You've also given the LLM room to go off-track on a task that LLM room to go off-track on a task that LLM room to go off-track on a task that didn't need that freedom. On the other didn't need that freedom. On the other didn't need that freedom. On the other hand, using a workflow when an agent is hand, using a workflow when an agent is hand, using a workflow when an agent is needed creates a brittle system. It needed creates a brittle system. It needed creates a brittle system. It breaks on inputs you didn't anticipate. breaks on inputs you didn't anticipate. breaks on inputs you didn't anticipate. It can't handle edge cases. It needs It can't handle edge cases. It needs It can't handle edge cases. It needs constant updating every time constant updating every time constant updating every time requirements change slightly. If you're requirements change slightly. If you're requirements change slightly. If you're weighing both options, run through these weighing both options, run through these weighing both options, run through these four questions. They'll point you in the four questions. They'll point you in the four questions. They'll point you in the right direction. First, can I define all right direction. First, can I define all right direction. First, can I define all possible paths in advance? If yes, possible paths in advance? If yes, possible paths in advance? If yes, choose workflow. If no, choose agent. choose workflow. If no, choose agent. choose workflow. If no, choose agent. Second, does the LLM need to make Second, does the LLM need to make Second, does the LLM need to make decisions about what to do next? If yes, decisions about what to do next? If yes, decisions about what to do next? If yes, choose agent. If no, choose workflow. choose agent. If no, choose workflow. choose agent. If no, choose workflow. Third, is this a well-defined, Third, is this a well-defined, Third, is this a well-defined, repeatable task? If yes, choose repeatable task? If yes, choose repeatable task? If yes, choose workflow. If no, choose agent. Fourth, workflow. If no, choose agent. Fourth, workflow. If no, choose agent. Fourth, does cost need to be strictly does cost need to be strictly does cost need to be strictly controlled? If yes, choose workflow, or controlled? If yes, choose workflow, or controlled? If yes, choose workflow, or at least add guardrails to the agent. at least add guardrails to the agent. at least add guardrails to the agent. Answer these four questions, and you'll Answer these four questions, and you'll Answer these four questions, and you'll know which tool to build. And here's the know which tool to build. And here's the know which tool to build. And here's the thing. The best systems often combine thing. The best systems often combine thing. The best systems often combine both. A workflow handles the predictable both. A workflow handles the predictable both. A workflow handles the predictable parts. An agent handles the parts that parts. An agent handles the parts that parts. An agent handles the parts that need flexibility. You don't have to need flexibility. You don't have to need flexibility. You don't have to choose just one. Start simple. Use a choose just one. Start simple. Use a choose just one. Start simple. Use a workflow. Add an agent when the workflow workflow. Add an agent when the workflow workflow. Add an agent when the workflow can't handle what you're throwing at it. can't handle what you're throwing at it. can't handle what you're throwing at it. Mix and match. The principle is the Mix and match. The principle is the Mix and match. The principle is the same. Start with what works, and only same. Start with what works, and only same. Start with what works, and only add complexity when you need it. Here's add complexity when you need it. Here's add complexity when you need it. Here's what you need to remember. Workflows what you need to remember. Workflows what you need to remember. Workflows follow a path you designed. Agents follow a path you designed. Agents follow a path you designed. Agents decide their own path. Most tasks need a decide their own path. Most tasks need a decide their own path. Most tasks need a workflow. Some tasks need an agent. The workflow. Some tasks need an agent. The workflow. Some tasks need an agent. The wrong choice costs you money, time, and wrong choice costs you money, time, and wrong choice costs you money, time, and debugging headaches. debugging headaches. debugging headaches. Use the checklist. Start simple, and Use the checklist. Start simple, and Use the checklist. Start simple, and only increase complexity when the only increase complexity when the only increase complexity when the simpler approach genuinely can't do the simpler approach genuinely can't do the simpler approach genuinely can't do the job. You've now learned about agents, job. You've now learned about agents, job. You've now learned about agents, workflows, tools, and patterns. You know workflows, tools, and patterns. You know workflows, tools, and patterns. You know how they work and when to use them. Next how they work and when to use them. Next how they work and when to use them. Next lesson, we're going to put it all lesson, we're going to put it all lesson, we're going to put it all together. together. together. I hope you're enjoying the course and I hope you're enjoying the course and I hope you're enjoying the course and the hands-on labs so far. the hands-on labs so far. the hands-on labs so far. If you're finding this learning If you're finding this learning If you're finding this learning experience valuable, please consider experience valuable, please consider experience valuable, please consider supporting us by subscribing to supporting us by subscribing to supporting us by subscribing to CodeCloud. For just a couple of hundred CodeCloud. For just a couple of hundred CodeCloud. For just a couple of hundred dollars a year, you get full access to dollars a year, you get full access to dollars a year, you get full access to this course, plus over 200 other courses this course, plus over 200 other courses this course, plus over 200 other courses covering everything from the covering everything from the covering everything from the fundamentals of uh computer science, fundamentals of uh computer science, fundamentals of uh computer science, DevOps to MLOps, cloud computing, and DevOps to MLOps, cloud computing, and DevOps to MLOps, cloud computing, and AI. AI. AI. Every course is hands-on and comes with Every course is hands-on and comes with Every course is hands-on and comes with built-in lab access. If you're not sure built-in lab access. If you're not sure built-in lab access. If you're not sure where to start, our curated learning where to start, our curated learning where to start, our curated learning paths guide you step by step through the paths guide you step by step through the paths guide you step by step through the specific career tracks, whether that's specific career tracks, whether that's specific career tracks, whether that's DevOps engineer, cloud architect, or AI DevOps engineer, cloud architect, or AI DevOps engineer, cloud architect, or AI engineer. So, you always know what to engineer. So, you always know what to engineer. So, you always know what to learn next. You'll also get access to learn next. You'll also get access to learn next. You'll also get access to playgrounds. These are sandboxed cloud playgrounds. These are sandboxed cloud playgrounds. These are sandboxed cloud environments across all three major environments across all three major environments across all three major platforms, AWS, Azure, and Google Cloud. platforms, AWS, Azure, and Google Cloud. platforms, AWS, Azure, and Google Cloud. Plus, CodeKeys gives you API keys to Plus, CodeKeys gives you API keys to Plus, CodeKeys gives you API keys to experiment with AI models hands-on. experiment with AI models hands-on. experiment with AI models hands-on. Every lab comes with an AI assistant Every lab comes with an AI assistant Every lab comes with an AI assistant built right in. So, if you get stuck, built right in. So, if you get stuck, built right in. So, if you get stuck, you get immediate, context-aware help you get immediate, context-aware help you get immediate, context-aware help without leaving the exercise. And our AI without leaving the exercise. And our AI without leaving the exercise. And our AI tutor takes it further with personalized tutor takes it further with personalized tutor takes it further with personalized learning that adapts to your pace and learning that adapts to your pace and learning that adapts to your pace and fills in your knowledge gaps. fills in your knowledge gaps. fills in your knowledge gaps. If you need to review or revise, the If you need to review or revise, the If you need to review or revise, the CodeCloud Notes app gives you access to CodeCloud Notes app gives you access to CodeCloud Notes app gives you access to all the notes across all of our courses all the notes across all of our courses all the notes across all of our courses in one place. Your go-to reference for in one place. Your go-to reference for in one place. Your go-to reference for quick lookups and exam prep. quick lookups and exam prep. quick lookups and exam prep. And our job simulation platform, And our job simulation platform, And our job simulation platform, CodeCloud Engineer, drops you into a CodeCloud Engineer, drops you into a CodeCloud Engineer, drops you into a realistic work environment with daily realistic work environment with daily realistic work environment with daily tasks, so you're not just learning tasks, so you're not just learning tasks, so you're not just learning concepts, you're practicing what the job concepts, you're practicing what the job concepts, you're practicing what the job actually feels like. Programs like 100 actually feels like. Programs like 100 actually feels like. Programs like 100 Days of DevOps, 100 Days of Cloud, and Days of DevOps, 100 Days of Cloud, and Days of DevOps, 100 Days of Cloud, and the upcoming 100 Days of AI give you the upcoming 100 Days of AI give you the upcoming 100 Days of AI give you structured learning paths that keep you structured learning paths that keep you structured learning paths that keep you consistent and accountable. You're never consistent and accountable. You're never consistent and accountable. You're never learning alone, either. Join our active learning alone, either. Join our active learning alone, either. Join our active Discord community to connect with Discord community to connect with Discord community to connect with thousands of uh other learners and drop thousands of uh other learners and drop thousands of uh other learners and drop into weekly office hours to get your into weekly office hours to get your into weekly office hours to get your questions answered live uh by our questions answered live uh by our questions answered live uh by our instructors. instructors. instructors. We have uh cohorts that are running to We have uh cohorts that are running to We have uh cohorts that are running to help you crack certification exam help you crack certification exam help you crack certification exam together with uh thousands of others who together with uh thousands of others who together with uh thousands of others who are along the same journey. And with the are along the same journey. And with the are along the same journey. And with the CodeCloud mobile app, you can keep CodeCloud mobile app, you can keep CodeCloud mobile app, you can keep learning on the go, whether you're learning on the go, whether you're learning on the go, whether you're commuting, on a break, or just away from commuting, on a break, or just away from commuting, on a break, or just away from your desk. So, all of that for a your desk. So, all of that for a your desk. So, all of that for a fraction of the of cost of a traditional fraction of the of cost of a traditional fraction of the of cost of a traditional college degree or an online bootcamp. college degree or an online bootcamp. college degree or an online bootcamp. Because we believe learning shouldn't Because we believe learning shouldn't Because we believe learning shouldn't cost you a fortune. &gt;&gt; [music]
