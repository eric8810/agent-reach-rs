Your AI agent needs a web browser, maybe Your AI agent needs a web browser, maybe Your AI agent needs a web browser, maybe it needs to scrape a website, fill out a it needs to scrape a website, fill out a it needs to scrape a website, fill out a form, or test your app's UI. And right form, or test your app's UI. And right form, or test your app's UI. And right now, three open-source tools dominate now, three open-source tools dominate now, three open-source tools dominate this space. Playwright CLI, Browser Use, this space. Playwright CLI, Browser Use, this space. Playwright CLI, Browser Use, and Agent Browser. They all let an AI and Agent Browser. They all let an AI and Agent Browser. They all let an AI control a browser, but they are built control a browser, but they are built control a browser, but they are built for very different things, and picking for very different things, and picking for very different things, and picking the wrong one means you are either the wrong one means you are either the wrong one means you are either fighting the tool or rewriting your fighting the tool or rewriting your fighting the tool or rewriting your setup later. setup later. setup later. So, let's break down exactly what each So, let's break down exactly what each So, let's break down exactly what each one does, how it works under the hood, one does, how it works under the hood, one does, how it works under the hood, and which one you should actually use. and which one you should actually use. and which one you should actually use. Before we get into it, a quick Before we get into it, a quick Before we get into it, a quick orientation. Playwright CLI is from orientation. Playwright CLI is from orientation. Playwright CLI is from Microsoft. It is a command-line Microsoft. It is a command-line Microsoft. It is a command-line interface that wraps the Playwright interface that wraps the Playwright interface that wraps the Playwright browser automation library, and it is browser automation library, and it is browser automation library, and it is designed specifically for coding agents designed specifically for coding agents designed specifically for coding agents like Claude Code or GitHub Copilot. like Claude Code or GitHub Copilot. like Claude Code or GitHub Copilot. Browser Use is a Python library from an Browser Use is a Python library from an Browser Use is a Python library from an independent team. It is a full agent independent team. It is a full agent independent team. It is a full agent framework where the AI drives the framework where the AI drives the framework where the AI drives the browser autonomously, making decisions browser autonomously, making decisions browser autonomously, making decisions step-by-step. step-by-step. step-by-step. An Agent Browser is from Vercel Labs. It An Agent Browser is from Vercel Labs. It An Agent Browser is from Vercel Labs. It is a standalone CLI written in Rust that is a standalone CLI written in Rust that is a standalone CLI written in Rust that talks directly to Chrome over the talks directly to Chrome over the talks directly to Chrome over the DevTools protocol. DevTools protocol. DevTools protocol. Three very different architectures. Three very different architectures. Three very different architectures. Let's start with how each one actually Let's start with how each one actually Let's start with how each one actually controls the browser. Playwright CLI is controls the browser. Playwright CLI is controls the browser. Playwright CLI is the simplest to understand the simplest to understand the simplest to understand architecturally. The entire entry point architecturally. The entire entry point architecturally. The entire entry point is literally one line of JavaScript. It is literally one line of JavaScript. It is literally one line of JavaScript. It just requires Playwright's internal CLI just requires Playwright's internal CLI just requires Playwright's internal CLI client program. That is it. All the client program. That is it. All the client program. That is it. All the heavy lifting happens inside Playwright heavy lifting happens inside Playwright heavy lifting happens inside Playwright itself. When you run a command like itself. When you run a command like itself. When you run a command like playwright crit playwright g playwright crit playwright g playwright crit playwright g example.com, example.com, example.com, it launches a Chromium instance through it launches a Chromium instance through it launches a Chromium instance through Playwright's browser management system, Playwright's browser management system, Playwright's browser management system, creates a page context, and gives you creates a page context, and gives you creates a page context, and gives you back a snapshot of the page state. Every back a snapshot of the page state. Every back a snapshot of the page state. Every command you run after that, click, fill, command you run after that, click, fill, command you run after that, click, fill, type, they all go through Playwright's type, they all go through Playwright's type, they all go through Playwright's locator API under the hood. The key locator API under the hood. The key locator API under the hood. The key design decision here is that it is meant design decision here is that it is meant design decision here is that it is meant to be token-efficient. Instead of to be token-efficient. Instead of to be token-efficient. Instead of loading a big tool schema into the AI's loading a big tool schema into the AI's loading a big tool schema into the AI's context window, the agent just calls CLI context window, the agent just calls CLI context window, the agent just calls CLI commands. Each command returns a compact commands. Each command returns a compact commands. Each command returns a compact snapshot showing element references like snapshot showing element references like snapshot showing element references like E1, E2, E3, and the agent uses those E1, E2, E3, and the agent uses those E1, E2, E3, and the agent uses those refs in the next command. Browser Use refs in the next command. Browser Use refs in the next command. Browser Use takes a completely different approach. takes a completely different approach. takes a completely different approach. It is not a CLI at all. It is an async It is not a CLI at all. It is an async It is not a CLI at all. It is an async Python framework where you give the Python framework where you give the Python framework where you give the agent a task in natural language, and it agent a task in natural language, and it agent a task in natural language, and it runs a loop. Each iteration of that loop runs a loop. Each iteration of that loop runs a loop. Each iteration of that loop captures a screenshot, extracts the DOM captures a screenshot, extracts the DOM captures a screenshot, extracts the DOM tree, builds an accessibility tree, tree, builds an accessibility tree, tree, builds an accessibility tree, serializes the interactive elements, serializes the interactive elements, serializes the interactive elements, sends all of that to an LLM, and then sends all of that to an LLM, and then sends all of that to an LLM, and then the LLM decides what action to take the LLM decides what action to take the LLM decides what action to take next. The agent loop is about 2,700 next. The agent loop is about 2,700 next. The agent loop is about 2,700 lines of Python in the service file, and lines of Python in the service file, and lines of Python in the service file, and it handles things like loop detection, it handles things like loop detection, it handles things like loop detection, planning, memory message compaction, and planning, memory message compaction, and planning, memory message compaction, and goal evaluation. It is a full autonomous goal evaluation. It is a full autonomous goal evaluation. It is a full autonomous agent framework, not just a browser agent framework, not just a browser agent framework, not just a browser driver. Under the hood, Browser Use driver. Under the hood, Browser Use driver. Under the hood, Browser Use talks to Chrome through the Chrome talks to Chrome through the Chrome talks to Chrome through the Chrome DevTools protocol, or CDP, using a typed DevTools protocol, or CDP, using a typed DevTools protocol, or CDP, using a typed Python wrapper called CDP Use. It fires Python wrapper called CDP Use. It fires Python wrapper called CDP Use. It fires off parallel CDP requests to capture DOM off parallel CDP requests to capture DOM off parallel CDP requests to capture DOM snapshots, the full accessibility tree, snapshots, the full accessibility tree, snapshots, the full accessibility tree, layout metrics, and even JavaScript layout metrics, and even JavaScript layout metrics, and even JavaScript event listeners all at once. Then it event listeners all at once. Then it event listeners all at once. Then it merges accessibility trees from every merges accessibility trees from every merges accessibility trees from every frame on the page into a single array. frame on the page into a single array. frame on the page into a single array. It runs JavaScript to detect which It runs JavaScript to detect which It runs JavaScript to detect which elements have click handlers, which ones elements have click handlers, which ones elements have click handlers, which ones have cursor pointer styles, and which have cursor pointer styles, and which have cursor pointer styles, and which are focusable. It builds a serialized are focusable. It builds a serialized are focusable. It builds a serialized tree where interactive elements are tree where interactive elements are tree where interactive elements are marked with their back-end node IDs in marked with their back-end node IDs in marked with their back-end node IDs in square brackets, and that whole square brackets, and that whole square brackets, and that whole representation gets sent to the LLM representation gets sent to the LLM representation gets sent to the LLM alongside a screenshot. Agent Browser is alongside a screenshot. Agent Browser is alongside a screenshot. Agent Browser is the most interesting architecturally. It the most interesting architecturally. It the most interesting architecturally. It is written entirely in Rust. The CLI is is written entirely in Rust. The CLI is is written entirely in Rust. The CLI is a thin client that sends JSON commands a thin client that sends JSON commands a thin client that sends JSON commands over a Unix socket to a daemon process. over a Unix socket to a daemon process. over a Unix socket to a daemon process. That daemon maintains a direct CDP That daemon maintains a direct CDP That daemon maintains a direct CDP WebSocket connection to Chrome. There is WebSocket connection to Chrome. There is WebSocket connection to Chrome. There is no node.js in the stack at all. No no node.js in the stack at all. No no node.js in the stack at all. No Playwright, no Puppeteer, just Rust Playwright, no Puppeteer, just Rust Playwright, no Puppeteer, just Rust talking raw Chrome DevTools protocol. talking raw Chrome DevTools protocol. talking raw Chrome DevTools protocol. The daemon stays alive between commands. The daemon stays alive between commands. The daemon stays alive between commands. So, when you run Agent Browser snapshot So, when you run Agent Browser snapshot So, when you run Agent Browser snapshot followed by Agent Browser click at E1, followed by Agent Browser click at E1, followed by Agent Browser click at E1, the second command executes almost the second command executes almost the second command executes almost instantly because the browser connection instantly because the browser connection instantly because the browser connection is already warm. The daemon uses about 7 is already warm. The daemon uses about 7 is already warm. The daemon uses about 7 MB of memory compared to around 140 MB MB of memory compared to around 140 MB MB of memory compared to around 140 MB for a node.js-based for a node.js-based for a node.js-based approach. For serverless deployments or approach. For serverless deployments or approach. For serverless deployments or containers where you are paying per containers where you are paying per containers where you are paying per megabyte, that matters. Now, let's talk megabyte, that matters. Now, let's talk megabyte, that matters. Now, let's talk about what the AI actually sees when it about what the AI actually sees when it about what the AI actually sees when it looks at a page. This is where the tools looks at a page. This is where the tools looks at a page. This is where the tools diverge the most, and it is the thing diverge the most, and it is the thing diverge the most, and it is the thing that determines how well your agent will that determines how well your agent will that determines how well your agent will actually perform. Playwright CLI gives actually perform. Playwright CLI gives actually perform. Playwright CLI gives the agent a YAML snapshot after every the agent a YAML snapshot after every the agent a YAML snapshot after every command. The snapshot contains element command. The snapshot contains element command. The snapshot contains element references like E1, E2, each mapped to a references like E1, E2, each mapped to a references like E1, E2, each mapped to a semantic description based on semantic description based on semantic description based on Playwright's locator system. So, you Playwright's locator system. So, you Playwright's locator system. So, you might see something like E1 is a button might see something like E1 is a button might see something like E1 is a button labeled submit, E2 is a text box labeled labeled submit, E2 is a text box labeled labeled submit, E2 is a text box labeled email. The agent reads that, decides email. The agent reads that, decides email. The agent reads that, decides what to do, and issues the next CLI what to do, and issues the next CLI what to do, and issues the next CLI command using those refs. It is clean, command using those refs. It is clean, command using those refs. It is clean, compact, and works well for coding compact, and works well for coding compact, and works well for coding agents that are already good at reading agents that are already good at reading agents that are already good at reading structured text. Screenshots are structured text. Screenshots are structured text. Screenshots are available, but the Read Me says they are available, but the Read Me says they are available, but the Read Me says they are rarely used since the snapshot is more rarely used since the snapshot is more rarely used since the snapshot is more common. Browser Use sends the LLM a much common. Browser Use sends the LLM a much common. Browser Use sends the LLM a much richer representation. It serializes the richer representation. It serializes the richer representation. It serializes the full DOM tree with tab indentation, full DOM tree with tab indentation, full DOM tree with tab indentation, marking interactive elements with their marking interactive elements with their marking interactive elements with their back-end node IDs. It includes detailed back-end node IDs. It includes detailed back-end node IDs. It includes detailed attributes like placeholder text, area attributes like placeholder text, area attributes like placeholder text, area labels, validation patterns, min and max labels, validation patterns, min and max labels, validation patterns, min and max values, autocomplete hints, and even values, autocomplete hints, and even values, autocomplete hints, and even computed styles for visibility checking. computed styles for visibility checking. computed styles for visibility checking. It also sends a plain screenshot It also sends a plain screenshot It also sends a plain screenshot alongside the text representation. The alongside the text representation. The alongside the text representation. The LLM gets both modalities and can reason LLM gets both modalities and can reason LLM gets both modalities and can reason about what it sees visually and about what it sees visually and about what it sees visually and structurally. Browser Use also has a structurally. Browser Use also has a structurally. Browser Use also has a concept of new elements, marking them concept of new elements, marking them concept of new elements, marking them with an asterisk so the agent knows what with an asterisk so the agent knows what with an asterisk so the agent knows what changed since the last step. Agent changed since the last step. Agent changed since the last step. Agent Browser builds its representation from Browser builds its representation from Browser builds its representation from the accessibility tree using the CDP the accessibility tree using the CDP the accessibility tree using the CDP accessibility API. It creates a accessibility API. It creates a accessibility API. It creates a hierarchical text tree where each hierarchical text tree where each hierarchical text tree where each interactive element gets a sequential interactive element gets a sequential interactive element gets a sequential ref like E1, E2, but it has a feature ref like E1, E2, but it has a feature ref like E1, E2, but it has a feature the other two do not, annotated the other two do not, annotated the other two do not, annotated screenshots. When you pass the annotate screenshots. When you pass the annotate screenshots. When you pass the annotate flag, Agent Browser overlays numbered flag, Agent Browser overlays numbered flag, Agent Browser overlays numbered labels directly on the screenshot image, labels directly on the screenshot image, labels directly on the screenshot image, and those numbers correspond to the and those numbers correspond to the and those numbers correspond to the element refs in the text snapshot. So, a element refs in the text snapshot. So, a element refs in the text snapshot. So, a vision-capable model can look at the vision-capable model can look at the vision-capable model can look at the screenshot, see a button labeled number screenshot, see a button labeled number screenshot, see a button labeled number three, and know that clicking ref E3 three, and know that clicking ref E3 three, and know that clicking ref E3 will hit that button. It bridges the gap will hit that button. It bridges the gap will hit that button. It bridges the gap between text and vision in a way that between text and vision in a way that between text and vision in a way that neither of the other tools do natively. neither of the other tools do natively. neither of the other tools do natively. Let's move on to the question a lot of Let's move on to the question a lot of Let's move on to the question a lot of people care about, persistent sessions people care about, persistent sessions people care about, persistent sessions and logging into web apps. All three and logging into web apps. All three and logging into web apps. All three tools support this, but they handle it tools support this, but they handle it tools support this, but they handle it differently. Playwright CLI has two differently. Playwright CLI has two differently. Playwright CLI has two modes. By default, it runs in memory, modes. By default, it runs in memory, modes. By default, it runs in memory, meaning everything disappears when the meaning everything disappears when the meaning everything disappears when the browser closes. But if you pass the browser closes. But if you pass the browser closes. But if you pass the persistent flag or specify a profile persistent flag or specify a profile persistent flag or specify a profile path, it writes browser state to disk. path, it writes browser state to disk. path, it writes browser state to disk. You can also explicitly save and load You can also explicitly save and load You can also explicitly save and load storage state, meaning cookies and local storage state, meaning cookies and local storage state, meaning cookies and local storage, to a JSON file using the state storage, to a JSON file using the state storage, to a JSON file using the state save and state load commands. save and state load commands. save and state load commands. That file contains all your cookies, That file contains all your cookies, That file contains all your cookies, session tokens, and local storage session tokens, and local storage session tokens, and local storage entries. You save it after logging in entries. You save it after logging in entries. You save it after logging in and load it before your next run. Simple and load it before your next run. Simple and load it before your next run. Simple and effective. Browser Use has the most and effective. Browser Use has the most and effective. Browser Use has the most options for persistence. It supports options for persistence. It supports options for persistence. It supports storage state files just like Playwright storage state files just like Playwright storage state files just like Playwright CLI. It supports user data directories CLI. It supports user data directories CLI. It supports user data directories for full browser profile persistence. It for full browser profile persistence. It for full browser profile persistence. It can connect to your actual system Chrome can connect to your actual system Chrome can connect to your actual system Chrome installation and inherit all your installation and inherit all your installation and inherit all your existing logins. It even has a cloud existing logins. It even has a cloud existing logins. It even has a cloud feature where you can upload your local feature where you can upload your local feature where you can upload your local Chrome profile to Browser Use cloud and Chrome profile to Browser Use cloud and Chrome profile to Browser Use cloud and have agents run against it remotely. And have agents run against it remotely. And have agents run against it remotely. And it integrates with PyOT for two-factor it integrates with PyOT for two-factor it integrates with PyOT for two-factor authentication, so your agent can handle authentication, so your agent can handle authentication, so your agent can handle TOTP codes during login flows. Agent TOTP codes during login flows. Agent TOTP codes during login flows. Agent Browser goes the furthest. It has four Browser goes the furthest. It has four Browser goes the furthest. It has four distinct persistence mechanisms. First, distinct persistence mechanisms. First, distinct persistence mechanisms. First, persistent profiles that store persistent profiles that store persistent profiles that store everything, cookies, IndexDB, service everything, cookies, IndexDB, service everything, cookies, IndexDB, service workers, cache, the works. Second, named workers, cache, the works. Second, named workers, cache, the works. Second, named sessions that auto-save and restore sessions that auto-save and restore sessions that auto-save and restore cookies and local storage to a sessions cookies and local storage to a sessions cookies and local storage to a sessions directory. Third, state files that can directory. Third, state files that can directory. Third, state files that can be encrypted with AES-256-GCM for secure credential storage. And for secure credential storage. And for secure credential storage. And fourth, an off vault where you save fourth, an off vault where you save fourth, an off vault where you save credentials locally, always encrypted, credentials locally, always encrypted, credentials locally, always encrypted, and the AI agent never sees the actual and the AI agent never sees the actual and the AI agent never sees the actual passwords. It just references a saved passwords. It just references a saved passwords. It just references a saved login by name. You can also auto-connect login by name. You can also auto-connect login by name. You can also auto-connect to an already running Chrome instance to an already running Chrome instance to an already running Chrome instance and piggyback on whatever sessions are and piggyback on whatever sessions are and piggyback on whatever sessions are already authenticated. Now, here is the already authenticated. Now, here is the already authenticated. Now, here is the question everyone who does web scraping question everyone who does web scraping question everyone who does web scraping wants answered. Which of these tools is wants answered. Which of these tools is wants answered. Which of these tools is least likely to get flagged as a bot? least likely to get flagged as a bot? least likely to get flagged as a bot? The honest answer is that none of them The honest answer is that none of them The honest answer is that none of them are stealth tools. They are all built are stealth tools. They are all built are stealth tools. They are all built for legitimate automation, not for for legitimate automation, not for for legitimate automation, not for evading detection. But they do differ in evading detection. But they do differ in evading detection. But they do differ in how detectable they are. how detectable they are. how detectable they are. Browser Use is the most aggressive about Browser Use is the most aggressive about Browser Use is the most aggressive about hiding automation signals. Its browser hiding automation signals. Its browser hiding automation signals. Its browser profile sets over 50 Chrome launch profile sets over 50 Chrome launch profile sets over 50 Chrome launch flags. The critical one is flags. The critical one is flags. The critical one is disable-blink-features=AutomationControlled. disable-blink-features=AutomationControlled. disable-blink-features=AutomationControlled. This flag tells Chrome not to set the This flag tells Chrome not to set the This flag tells Chrome not to set the navigator.webdriver property to true, which is the single property to true, which is the single property to true, which is the single most common way websites detect headless most common way websites detect headless most common way websites detect headless browsers. Browser use also disables browsers. Browser use also disables browsers. Browser use also disables dozens of Chrome components that could dozens of Chrome components that could dozens of Chrome components that could leak automation signals. Things like the leak automation signals. Things like the leak automation signals. Things like the info bars, speech synthesis API, desktop info bars, speech synthesis API, desktop info bars, speech synthesis API, desktop notifications, background networking, notifications, background networking, notifications, background networking, and client-side phishing detection. It and client-side phishing detection. It and client-side phishing detection. It also supports custom user agents and also supports custom user agents and also supports custom user agents and proxy rotation through its cloud proxy rotation through its cloud proxy rotation through its cloud service. Agent Browser takes a different service. Agent Browser takes a different service. Agent Browser takes a different approach. Instead of trying to hide approach. Instead of trying to hide approach. Instead of trying to hide automation signals with flags, it uses automation signals with flags, it uses automation signals with flags, it uses Chrome for Testing, which is Google's Chrome for Testing, which is Google's Chrome for Testing, which is Google's official browser built specifically official browser built specifically official browser built specifically designed for automation. Chrome for designed for automation. Chrome for designed for automation. Chrome for Testing is downloaded from Google's own Testing is downloaded from Google's own Testing is downloaded from Google's own servers and is a recognized legitimate servers and is a recognized legitimate servers and is a recognized legitimate automation channel. The theory is its automation channel. The theory is its automation channel. The theory is its sophisticated bot detection services are sophisticated bot detection services are sophisticated bot detection services are more likely to whitelist traffic from more likely to whitelist traffic from more likely to whitelist traffic from known automation tools than to be fooled known automation tools than to be fooled known automation tools than to be fooled by flag-based masking. Agent Browser by flag-based masking. Agent Browser by flag-based masking. Agent Browser also supports custom user agents, proxy also supports custom user agents, proxy also supports custom user agents, proxy servers, and device emulation, but it servers, and device emulation, but it servers, and device emulation, but it does not inject stealth scripts or does not inject stealth scripts or does not inject stealth scripts or monkey patch browser APIs. Playwright monkey patch browser APIs. Playwright monkey patch browser APIs. Playwright CLI inherits whatever Playwright does by CLI inherits whatever Playwright does by CLI inherits whatever Playwright does by default. Modern versions of Playwright default. Modern versions of Playwright default. Modern versions of Playwright do set the disable blink features do set the disable blink features do set the disable blink features automation controlled flag. So, automation controlled flag. So, automation controlled flag. So, navigator.webdriver navigator.webdriver navigator.webdriver will appear undefined rather than true. will appear undefined rather than true. will appear undefined rather than true. Beyond that, it supports custom user Beyond that, it supports custom user Beyond that, it supports custom user agents, proxy configuration, and device agents, proxy configuration, and device agents, proxy configuration, and device emulation through environment variables, emulation through environment variables, emulation through environment variables, but it does not go out of its way to add but it does not go out of its way to add but it does not go out of its way to add extra stealth. For serious web scraping extra stealth. For serious web scraping extra stealth. For serious web scraping where bot detection is a primary where bot detection is a primary where bot detection is a primary concern, all three tools recommend using concern, all three tools recommend using concern, all three tools recommend using their cloud or managed browser their cloud or managed browser their cloud or managed browser offerings. Browser use has Browser use offerings. Browser use has Browser use offerings. Browser use has Browser use cloud with proprietary stealth Chromium cloud with proprietary stealth Chromium cloud with proprietary stealth Chromium builds, captcha solving, and proxy builds, captcha solving, and proxy builds, captcha solving, and proxy rotation. Agent Browser integrates with rotation. Agent Browser integrates with rotation. Agent Browser integrates with Browserbase, Browserless, and other Browserbase, Browserless, and other Browserbase, Browserless, and other managed browser services that specialize managed browser services that specialize managed browser services that specialize in evasion. And Playwright CLI can in evasion. And Playwright CLI can in evasion. And Playwright CLI can connect to remote Playwright servers or connect to remote Playwright servers or connect to remote Playwright servers or CDP endpoints hosted by similar CDP endpoints hosted by similar CDP endpoints hosted by similar services. The bottom line on bot services. The bottom line on bot services. The bottom line on bot detection is this. detection is this. detection is this. If you are scraping sites that do not If you are scraping sites that do not If you are scraping sites that do not care about bots, any of these tools will care about bots, any of these tools will care about bots, any of these tools will work fine. If you are scraping sites work fine. If you are scraping sites work fine. If you are scraping sites with basic bot detection, Browser use's with basic bot detection, Browser use's with basic bot detection, Browser use's flag-based approach gives you the best flag-based approach gives you the best flag-based approach gives you the best out of the box experience. If you are out of the box experience. If you are out of the box experience. If you are scraping sites with serious anti-bot scraping sites with serious anti-bot scraping sites with serious anti-bot measures like Cloudflare or Data Dome, measures like Cloudflare or Data Dome, measures like Cloudflare or Data Dome, you need a managed browser service you need a managed browser service you need a managed browser service regardless of which tool you choose. regardless of which tool you choose. regardless of which tool you choose. Because no amount of Chrome flags will Because no amount of Chrome flags will Because no amount of Chrome flags will fool a sophisticated fingerprinting fool a sophisticated fingerprinting fool a sophisticated fingerprinting system. Now, let's talk about AI-driven system. Now, let's talk about AI-driven system. Now, let's talk about AI-driven UI testing. This is a use case that is UI testing. This is a use case that is UI testing. This is a use case that is growing fast, and the three tools are growing fast, and the three tools are growing fast, and the three tools are surprisingly different here. Playwright surprisingly different here. Playwright surprisingly different here. Playwright CLI has the strongest testing story CLI has the strongest testing story CLI has the strongest testing story because it is literally built on because it is literally built on because it is literally built on Playwright, which is one of the most Playwright, which is one of the most Playwright, which is one of the most popular testing frameworks in existence. popular testing frameworks in existence. popular testing frameworks in existence. It generates Playwright TypeScript test It generates Playwright TypeScript test It generates Playwright TypeScript test code automatically as you interact with code automatically as you interact with code automatically as you interact with the browser. When the agent fills a text the browser. When the agent fills a text the browser. When the agent fills a text box, the generated code includes the box, the generated code includes the box, the generated code includes the semantic locator like page.get by role semantic locator like page.get by role semantic locator like page.get by role text box with the proper name. Those text box with the proper name. Those text box with the proper name. Those generated tests use resilient selectors generated tests use resilient selectors generated tests use resilient selectors that survive DOM changes, which is that survive DOM changes, which is that survive DOM changes, which is exactly what you want for maintainable exactly what you want for maintainable exactly what you want for maintainable test suites. It also has full tracing test suites. It also has full tracing test suites. It also has full tracing support. You can start a trace, run support. You can start a trace, run support. You can start a trace, run through a workflow, and stop the trace, through a workflow, and stop the trace, through a workflow, and stop the trace, and it captures DOM snapshots, and it captures DOM snapshots, and it captures DOM snapshots, screenshots, network activity, and screenshots, network activity, and screenshots, network activity, and console logs at every step. The trace console logs at every step. The trace console logs at every step. The trace files can be opened in Playwright's files can be opened in Playwright's files can be opened in Playwright's trace viewer for detailed debugging. trace viewer for detailed debugging. trace viewer for detailed debugging. Agent Browser has the best visual Agent Browser has the best visual Agent Browser has the best visual regression testing capabilities. It has regression testing capabilities. It has regression testing capabilities. It has built-in diff commands for comparing built-in diff commands for comparing built-in diff commands for comparing accessibility tree snapshots, accessibility tree snapshots, accessibility tree snapshots, pixel-level screenshot comparisons with pixel-level screenshot comparisons with pixel-level screenshot comparisons with configurable color thresholds, and even configurable color thresholds, and even configurable color thresholds, and even URL-to-URL visual comparison where it URL-to-URL visual comparison where it URL-to-URL visual comparison where it opens two pages and diffs them side by opens two pages and diffs them side by opens two pages and diffs them side by side. For visual QA workflows where you side. For visual QA workflows where you side. For visual QA workflows where you need to catch unintended layout changes, need to catch unintended layout changes, need to catch unintended layout changes, this is exactly what you want. It also this is exactly what you want. It also this is exactly what you want. It also has state check commands, is visible, is has state check commands, is visible, is has state check commands, is visible, is enabled, is checked that return boolean enabled, is checked that return boolean enabled, is checked that return boolean values an agent can use for basic values an agent can use for basic values an agent can use for basic assertions. Browser use is not really a assertions. Browser use is not really a assertions. Browser use is not really a testing tool. It is an autonomous agent testing tool. It is an autonomous agent testing tool. It is an autonomous agent framework. You could theoretically use framework. You could theoretically use framework. You could theoretically use it for testing by giving the agent a it for testing by giving the agent a it for testing by giving the agent a task like navigate to the checkout page task like navigate to the checkout page task like navigate to the checkout page and verify the total is correct, but it and verify the total is correct, but it and verify the total is correct, but it does not generate test code, it does not does not generate test code, it does not does not generate test code, it does not have built-in assertions, and it does have built-in assertions, and it does have built-in assertions, and it does not support visual regression out of the not support visual regression out of the not support visual regression out of the box. Where it shines is in scenarios box. Where it shines is in scenarios box. Where it shines is in scenarios where the AI needs to figure out the where the AI needs to figure out the where the AI needs to figure out the workflow on its own, like filling out a workflow on its own, like filling out a workflow on its own, like filling out a complex multi-step form or navigating an complex multi-step form or navigating an complex multi-step form or navigating an unfamiliar website. Let's talk about the unfamiliar website. Let's talk about the unfamiliar website. Let's talk about the LLM integration story cuz this is where LLM integration story cuz this is where LLM integration story cuz this is where Browser use really stands apart. Browser Browser use really stands apart. Browser Browser use really stands apart. Browser use supports 18 different LLM providers use supports 18 different LLM providers use supports 18 different LLM providers out of the box. out of the box. out of the box. OpenAI, Anthropic, Google Gemini, Grok, OpenAI, Anthropic, Google Gemini, Grok, OpenAI, Anthropic, Google Gemini, Grok, Ollama for local models, Mistral, Azure, Ollama for local models, Mistral, Azure, Ollama for local models, Mistral, Azure, Deep Seek, Cerebras, and more. It also Deep Seek, Cerebras, and more. It also Deep Seek, Cerebras, and more. It also has its own fine-tuned model called BE20 has its own fine-tuned model called BE20 has its own fine-tuned model called BE20 that is optimized specifically for that is optimized specifically for that is optimized specifically for browser interaction. The team claims it browser interaction. The team claims it browser interaction. The team claims it is three to five times more accurate is three to five times more accurate is three to five times more accurate than general-purpose models for browser than general-purpose models for browser than general-purpose models for browser tasks. Each provider has its own adapter tasks. Each provider has its own adapter tasks. Each provider has its own adapter with model-specific optimizations like with model-specific optimizations like with model-specific optimizations like Anthropic's native thinking mode for Anthropic's native thinking mode for Anthropic's native thinking mode for extended reasoning or adjusted timeout extended reasoning or adjusted timeout extended reasoning or adjusted timeout settings for different model speeds. The settings for different model speeds. The settings for different model speeds. The agent loop in Browser use is agent loop in Browser use is agent loop in Browser use is sophisticated. It tracks the agent's sophisticated. It tracks the agent's sophisticated. It tracks the agent's internal memory, does multi-step internal memory, does multi-step internal memory, does multi-step planning with progress tracking, detects planning with progress tracking, detects planning with progress tracking, detects when the agent is stuck in a loop by when the agent is stuck in a loop by when the agent is stuck in a loop by hashing action sequences and page hashing action sequences and page hashing action sequences and page fingerprints, compacts old conversation fingerprints, compacts old conversation fingerprints, compacts old conversation history to keep the prompt size history to keep the prompt size history to keep the prompt size manageable, and even has an optional manageable, and even has an optional manageable, and even has an optional judge system where a separate LLM judge system where a separate LLM judge system where a separate LLM evaluates whether the task was completed evaluates whether the task was completed evaluates whether the task was completed successfully. Playwright CLI and Agent successfully. Playwright CLI and Agent successfully. Playwright CLI and Agent Browser do not include any LLM Browser do not include any LLM Browser do not include any LLM integration. They are browser tools, not integration. They are browser tools, not integration. They are browser tools, not agent frameworks. They expect the AI agent frameworks. They expect the AI agent frameworks. They expect the AI model to be the caller, not the callee. model to be the caller, not the callee. model to be the caller, not the callee. A coding agent like Claude Code or A coding agent like Claude Code or A coding agent like Claude Code or Cursor calls Playwright CLI commands Cursor calls Playwright CLI commands Cursor calls Playwright CLI commands just like it would call any other CLI just like it would call any other CLI just like it would call any other CLI tool. Similarly, Agent Browser is tool. Similarly, Agent Browser is tool. Similarly, Agent Browser is designed to be invoked by an external designed to be invoked by an external designed to be invoked by an external agent or orchestration framework. This agent or orchestration framework. This agent or orchestration framework. This makes them more flexible in some ways, makes them more flexible in some ways, makes them more flexible in some ways, since you are not locked into any since you are not locked into any since you are not locked into any particular LLM provider or agent loop particular LLM provider or agent loop particular LLM provider or agent loop design, but it also means you have to design, but it also means you have to design, but it also means you have to build the agent logic yourself. Let me build the agent logic yourself. Let me build the agent logic yourself. Let me talk about a few more practical talk about a few more practical talk about a few more practical differences that matter when you are differences that matter when you are differences that matter when you are choosing between these tools. choosing between these tools. choosing between these tools. Multi-tab support. All three support Multi-tab support. All three support Multi-tab support. All three support multiple tabs. Playwright CLI has multiple tabs. Playwright CLI has multiple tabs. Playwright CLI has explicit tab commands, tab new, tab explicit tab commands, tab new, tab explicit tab commands, tab new, tab close, tab select. Agent Browser close, tab select. Agent Browser close, tab select. Agent Browser supports named sessions where each supports named sessions where each supports named sessions where each session is an isolated browser instance session is an isolated browser instance session is an isolated browser instance with its own cookies and history, and with its own cookies and history, and with its own cookies and history, and you can run on multiple sessions you can run on multiple sessions you can run on multiple sessions concurrently. Browser use tracks concurrently. Browser use tracks concurrently. Browser use tracks multiple CDP targets and manages them multiple CDP targets and manages them multiple CDP targets and manages them through its session manager. Network through its session manager. Network through its session manager. Network interception. Playwright CLI exposes interception. Playwright CLI exposes interception. Playwright CLI exposes Playwright's route API, letting you mock Playwright's route API, letting you mock Playwright's route API, letting you mock responses, block requests, or simulate responses, block requests, or simulate responses, block requests, or simulate network errors like connection refused network errors like connection refused network errors like connection refused or timed out. Agent Browser has full HAR or timed out. Agent Browser has full HAR or timed out. Agent Browser has full HAR recording, request tracking with recording, request tracking with recording, request tracking with filtering by type and status, and route filtering by type and status, and route filtering by type and status, and route commands for mocking. commands for mocking. commands for mocking. Browser use does not expose network Browser use does not expose network Browser use does not expose network interception directly to the agent, interception directly to the agent, interception directly to the agent, though it has internal network handling though it has internal network handling though it has internal network handling through its watchdog system. Debugging through its watchdog system. Debugging through its watchdog system. Debugging and observability. and observability. and observability. Agent Browser has a Next.js Agent Browser has a Next.js Agent Browser has a Next.js observability dashboard that shows live observability dashboard that shows live observability dashboard that shows live viewport frames over WebSocket, command viewport frames over WebSocket, command viewport frames over WebSocket, command activity with timing, and console activity with timing, and console activity with timing, and console messages. It is accessible at messages. It is accessible at messages. It is accessible at localhost:4848 and gives you a real-time window into and gives you a real-time window into and gives you a real-time window into what the agent is doing. Playwright CLI what the agent is doing. Playwright CLI what the agent is doing. Playwright CLI has tracing and video recording. Browser has tracing and video recording. Browser has tracing and video recording. Browser use has telemetry and GIF generation of use has telemetry and GIF generation of use has telemetry and GIF generation of the agent session, along the agent session, along the agent session, along with cost tracking that shows you how with cost tracking that shows you how with cost tracking that shows you how many tokens and many tokens and many tokens and task consumed. Docker and deployment. task consumed. Docker and deployment. task consumed. Docker and deployment. Browser use ships with two Docker files, Browser use ships with two Docker files, Browser use ships with two Docker files, a standard one and a fast one that uses a standard one and a fast one that uses a standard one and a fast one that uses a pre-built base image. It runs as a a pre-built base image. It runs as a a pre-built base image. It runs as a non-root user and exposes CDP on port non-root user and exposes CDP on port non-root user and exposes CDP on port 9242. 9242. 9242. Agent Browser compiles to a single Agent Browser compiles to a single Agent Browser compiles to a single native binary of about 20 megabytes, native binary of about 20 megabytes, native binary of about 20 megabytes, making it ideal for lightweight depends making it ideal for lightweight depends making it ideal for lightweight depends on Node.js and the Playwright package, on Node.js and the Playwright package, on Node.js and the Playwright package, which is a heavier footprint. Let me which is a heavier footprint. Let me which is a heavier footprint. Let me wrap up with clear recommendations for wrap up with clear recommendations for wrap up with clear recommendations for specific use cases. If you are building specific use cases. If you are building specific use cases. If you are building AI-powered UI tests and want to generate AI-powered UI tests and want to generate AI-powered UI tests and want to generate test code, use Playwright CLI. It is test code, use Playwright CLI. It is test code, use Playwright CLI. It is backed by Microsoft, built on the most backed by Microsoft, built on the most backed by Microsoft, built on the most popular browser testing framework, and popular browser testing framework, and popular browser testing framework, and generates real Playwright test code that generates real Playwright test code that generates real Playwright test code that you can drop into your test suite. The you can drop into your test suite. The you can drop into your test suite. The tracing and debugging tools are mature. tracing and debugging tools are mature. tracing and debugging tools are mature. For testing, this is the safest bet. If For testing, this is the safest bet. If For testing, this is the safest bet. If you want an autonomous AI agent that can you want an autonomous AI agent that can you want an autonomous AI agent that can navigate the web on its own with minimal navigate the web on its own with minimal navigate the web on its own with minimal hand-holding, use Browser use. The agent hand-holding, use Browser use. The agent hand-holding, use Browser use. The agent loop is battle-tested, it supports loop is battle-tested, it supports loop is battle-tested, it supports nearly every major LLM, and the built-in nearly every major LLM, and the built-in nearly every major LLM, and the built-in planning, memory, and loop detection planning, memory, and loop detection planning, memory, and loop detection mean the agent can handle complex mean the agent can handle complex mean the agent can handle complex multi-step workflows. If your use case multi-step workflows. If your use case multi-step workflows. If your use case is something like have the AI research is something like have the AI research is something like have the AI research competitors on their websites or have competitors on their websites or have competitors on their websites or have the AI fill out this government form, the AI fill out this government form, the AI fill out this government form, Browser use is built for exactly that. Browser use is built for exactly that. Browser use is built for exactly that. If you need a lightweight, fast, If you need a lightweight, fast, If you need a lightweight, fast, universal browser tool for AI agents in universal browser tool for AI agents in universal browser tool for AI agents in production, use Agent Browser. The Rust production, use Agent Browser. The Rust production, use Agent Browser. The Rust daemon is tiny and fast. The client daemon is tiny and fast. The client daemon is tiny and fast. The client daemon architecture means commands daemon architecture means commands daemon architecture means commands execute almost instantly after the first execute almost instantly after the first execute almost instantly after the first one, and the annotated screenshots with one, and the annotated screenshots with one, and the annotated screenshots with element refs are uniquely valuable for element refs are uniquely valuable for element refs are uniquely valuable for vision capable models. It is also the vision capable models. It is also the vision capable models. It is also the best choice for visual regression best choice for visual regression best choice for visual regression testing and for serverless or container testing and for serverless or container testing and for serverless or container deployments where memory and binary size deployments where memory and binary size deployments where memory and binary size matter. matter. matter. And honestly, these tools are not And honestly, these tools are not And honestly, these tools are not mutually exclusive. You might use mutually exclusive. You might use mutually exclusive. You might use Playwright CLI for testing in CI, Playwright CLI for testing in CI, Playwright CLI for testing in CI, Browser use for autonomous web tasks, Browser use for autonomous web tasks, Browser use for autonomous web tasks, and Agent Browser for production and Agent Browser for production and Agent Browser for production scraping pipelines. scraping pipelines. scraping pipelines. The ecosystem is still young and The ecosystem is still young and The ecosystem is still young and evolving fast, so the best approach is evolving fast, so the best approach is evolving fast, so the best approach is to pick the one that fits your immediate to pick the one that fits your immediate to pick the one that fits your immediate need and be ready to adapt as these need and be ready to adapt as these need and be ready to adapt as these projects mature. One last thing, all projects mature. One last thing, all projects mature. One last thing, all three of these projects are open source three of these projects are open source three of these projects are open source and actively maintained. Playwright CLI and actively maintained. Playwright CLI and actively maintained. Playwright CLI is Apache 2.0 under Microsoft. Browser is Apache 2.0 under Microsoft. Browser is Apache 2.0 under Microsoft. Browser use is MIT licensed. Agent Browser is use is MIT licensed. Agent Browser is use is MIT licensed. Agent Browser is Apache 2.0 under Versal Labs. Apache 2.0 under Versal Labs. Apache 2.0 under Versal Labs. Check them out on GitHub, and if you end Check them out on GitHub, and if you end Check them out on GitHub, and if you end up using one of them, consider starring up using one of them, consider starring up using one of them, consider starring the repo or contributing back. These the repo or contributing back. These the repo or contributing back. These teams are building the infrastructure teams are building the infrastructure teams are building the infrastructure for the next generation of the AI on the for the next generation of the AI on the for the next generation of the AI on the web, and they could use the support.
